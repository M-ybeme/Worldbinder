# Copy to .env and adjust as needed. Local defaults match infrastructure/compose/docker-compose.yml.

# --- Local infrastructure (docker compose) ---
POSTGRES_USER=worldbinder
POSTGRES_PASSWORD=worldbinder
POSTGRES_DB=worldbinder
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ROOT_USER=worldbinder
MINIO_ROOT_PASSWORD=worldbinder-dev-secret
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# --- apps/api ---
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
DATABASE_URL=postgres://worldbinder:worldbinder@localhost:5432/worldbinder
REDIS_URL=redis://localhost:6379

# CORS — comma-separated allow-list of frontend origins allowed to make
# credentialed cross-origin requests. Empty in development means "reflect
# any origin" (nothing to set here locally); required outside development —
# an empty list there means no cross-origin requests are allowed at all
# (fails closed) until the real deployed frontend's origin is set, e.g.:
# CORS_ORIGIN=https://app.example.com,https://staging.example.com
CORS_ORIGIN=

# Auth — generate a real secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=replace-with-a-random-32-byte-hex-string
JWT_ACCESS_TTL_SECONDS=900
REFRESH_TOKEN_TTL_DAYS=30
REFRESH_COOKIE_NAME=worldbinder_refresh
FRONTEND_URL=http://localhost:5173

# Mail — Mailpit locally (see infrastructure/compose/docker-compose.yml); swap for a real
# provider's SMTP relay in production — a config change, not a code change. Resend
# (smtp.resend.com) and Postmark (smtp.postmarkapp.com) both relay on port 587 with
# STARTTLS: set SMTP_HOST/SMTP_USER/SMTP_PASSWORD accordingly and leave SMTP_SECURE=false
# (nodemailer negotiates STARTTLS automatically when the server advertises it).
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_SECURE=false
MAIL_FROM=Worldbinder <noreply@worldbinder.local>

# Object storage — MinIO locally (same credentials as MINIO_ROOT_USER/PASSWORD
# above, distinct var names since apps/api and apps/worker both read these
# directly); swap for Cloudflare R2 or AWS S3 credentials in production — a
# config change, not a code change. For R2: STORAGE_ENDPOINT is your account's
# R2 S3 API endpoint, STORAGE_REGION=auto, STORAGE_FORCE_PATH_STYLE=false.
STORAGE_ENDPOINT=http://127.0.0.1:9000
STORAGE_REGION=us-east-1
STORAGE_BUCKET=worldbinder-dev
STORAGE_ACCESS_KEY_ID=worldbinder
STORAGE_SECRET_ACCESS_KEY=worldbinder-dev-secret
STORAGE_FORCE_PATH_STYLE=true

# Error monitoring — unset means Sentry is never initialized at all (fully inert), not
# initialized with an empty DSN. Set to a real project's DSN to enable it; apps/api and
# apps/worker both read this same var name directly.
SENTRY_DSN=

# --- apps/web ---
VITE_API_URL=http://localhost:3000
# Same "unset = fully inert" behavior as SENTRY_DSN above, read at build time by Vite.
VITE_SENTRY_DSN=
