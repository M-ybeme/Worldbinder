# Changelog

All notable changes to this project are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning follows [Semantic Versioning](https://semver.org/) — a pre-1.0 project, so `0.MINOR.PATCH` bumps for any user-visible or structural change, `PATCH` for fixes with no scope change.

Every push to `main` should add an entry here. This is meant to be an honest record of what actually shipped, not a restatement of the roadmap's aspirations — if something was attempted and reverted, or shipped partially, say so.

## [Unreleased]

## [0.14.9] - 2026-07-15

### Added

- **Milestone 14, Phase 9 — Storage: S3-compatible backend made swappable.** Audited `StorageService` and both env schemas against the goal of "R2 becomes a config change later, not a code change" — most of it was already correct: endpoint/region/bucket/credentials/path-style are all plain env vars with MinIO-shaped local defaults, not hardcoded logic, and `onModuleInit()`'s auto-bucket-creation already skips production correctly. Confirmed via a full-codebase grep that no real code path (only comments/local test fixtures) hardcodes "minio".
- **Found one real gap**: Phase 4b's dev-only-secrets guard (rejecting the well-known MinIO default storage credentials outside `development`/`test`) was applied to `apiEnvSchema` but never to `workerEnvSchema`, even though `apps/worker` talks to the same storage bucket directly. A production worker deploy that forgot to set real R2 credentials would have booted silently against those defaults.
- Extracted the guard into a shared `rejectDevOnlyValuesOutsideDevAndTest` function in `packages/config/src/env.ts` and applied it to both schemas. 4 new unit tests cover `workerEnvSchema`'s guard.
- Added a one-line `.env.example` note on the two R2-specific knobs (`STORAGE_REGION=auto`, `STORAGE_FORCE_PATH_STYLE=false`).
- Full suite re-run clean: 15 `packages/config` unit tests, 92 API unit tests, 189 API integration tests, 31 worker unit tests, 4 worker integration tests, typecheck/lint clean across all three packages.
- **Scope note**: this is Phase 9 of 13. Email/monitoring/backup/runbook work remains, tracked in the roadmap.

## [0.14.8] - 2026-07-15

### Added

- **Milestone 14, Phase 8 — Load tests.** No load-testing tool existed anywhere (`k6`/`autocannon`/`artillery` all absent) despite the roadmap already documenting concrete seed sizes and latency budgets to test against.
- Extended `seed-perf.ts` (Milestone 7) with the two §20.6 seed counts it was missing — 500 timeline events, 20 maps — plus a fixed login credential for its fixture owner user so HTTP harnesses can authenticate through the real `/auth/login` endpoint. Attachments still aren't seeded there: export's attachment step reads real bytes from object storage, and fake rows with no matching MinIO object would fail that step instead of measuring it.
- Installed `autocannon`. `apps/api/src/load-test/http-load-test.ts` (`pnpm --filter @worldbinder/api load-test:http`) runs real concurrent HTTP load against the genuinely concurrent read routes — campaigns list, dashboard, entity detail, relationships, search. `apps/worker/src/load-test/export-import-benchmark.ts` (`pnpm --filter @worldbinder/worker load-test:export-import`) times the real export/validate/import pipeline directly at full 10k/50k scale instead, since background jobs aren't a concurrent-request workload.
- **Found and fixed a real bug while wiring this up**: `seed-perf.ts`'s `main().catch(...)` ran unconditionally at module scope, so importing its exported constants (needed by the new scripts and by `search-benchmark.ts`) silently re-triggered a full campaign re-seed as a side effect — the first load-test run got its own campaign deleted and recreated mid-benchmark. Fixed with a `require.main === module` guard.
- **Surfaced a real, working-as-designed interaction with Phase 5's rate limiter**: `GlobalRateLimitGuard` buckets by client IP at 300 req/60s, and a loopback benchmark can't spread across multiple real IPs the way production traffic would, so a duration-based run got mostly 429'd after the first few requests. Switched the harness to request-count-bounded (`amount`, not `duration`) — 200 total requests across 5 targets, safely under the shared floor — and documented this as a deliberate scope note, not a defect to fix in Phase 5's code.
- **Results**: `GET /campaigns` p97.5=25ms, dashboard p97.5=199ms, entity detail p97.5=9ms, relationships p97.5=208ms, search p97.5=360ms — all PASS against the §22.2 budgets (reads <300ms, search <500ms). Export: 1.1s for an 8.5MB archive. Dry-run validate: 359ms, counts matched exactly (10000/50000/2000/200/500/20). Full transactional import: 45.3s — the dominant cost, expected at this row count in one transaction, and now a concrete number instead of a guess for §22.2's "may take longer" framing.
- Full suite re-run clean: 92 API unit tests, 31 worker unit tests, typecheck/lint on both.
- **Scope note**: this is Phase 8 of 13. The storage/email/monitoring/backup/runbook work remains, tracked in the roadmap.

## [0.14.7] - 2026-07-15

### Added

- **Milestone 14, Phase 7 — Bundle analysis.** No bundle-analysis tooling existed and `apps/web/src/routes/index.tsx` eagerly imported all ~30 page components into a single static router tree — directly contradicting the roadmap's own §22.1 targets ("lazy-load TipTap, map, and timeline bundles," "route-level code splitting"), documented in the roadmap audit but never implemented.
- Added `rollup-plugin-visualizer`, wired opt-in (`ANALYZE=1 pnpm build`, writing `dist/stats.html`) rather than always-on, since it's a build-time cost and the output is a debugging artifact CI doesn't need.
- Converted every leaf page route to react-router-dom v7's native `lazy: () => import(...).then(m => ({ Component: m.XyzPage }))` property. One mechanism covers both roadmap bullets at once, since lazy-loading pages that import TipTap/map/timeline transitively code-splits those dependencies too. Only the root shell, layout/guard components, and the `/` landing route stay eager.
- **Before/after** (production build): the single bundle dropped from **1,423.15 kB (400.80 kB gzip)** to a main/vendor chunk of **523.41 kB (157.14 kB gzip)** — a 61% cut in gzipped JS on the critical path — with TipTap/ProseMirror split into its own on-demand **496.73 kB (155.93 kB gzip)** chunk and ~40 small per-route chunks (0.1–15 kB each).
- Verified with clean `typecheck`/`lint` (confirms every lazy import's named export is correct), the existing Vitest suite (11/11), and a manual Playwright smoke pass against a real `vite preview` build of the output — logged in as the seeded demo user, navigated campaign overview/world/timeline/maps/sessions plus an entity detail and entity-creation page, confirmed the TipTap editor actually mounts at runtime from its lazy chunk, and saw no console/page errors or failed dynamic imports on any route.
- **Scope note**: this is Phase 7 of 13. Load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.6] - 2026-07-15

### Added

- **Milestone 14, Phase 6 — Database query profiling.** Every proposed index was verified against actual service-layer query code before being added, not just guessed from column names — three of the original audit's own findings (`securityEvents.userId`, `campaignImports.resultCampaignId`, `attachments.uploadedByUserId`) turned out to have no real query to benefit from and were deliberately skipped rather than blindly implemented.
- Migration `0013_sticky_klaw.sql` adds 17 indexes across 14 tables: `entityWikiLinks` (two composites, backing the delete-then-rewrite query and `backlinks()`), `campaignInvitations` (`campaignId`, `tokenHash`), `userSessions` (`userId`, `tokenFamilyId` — the refresh-reuse-detection lookup key), `emailVerificationTokens`/`passwordResetTokens.tokenHash` (the actual verify/reset lookup keys), `timelineEventEntities.entityId`, `timelineEventSessions.sessionId`, a trigram index on `timelineEvents.title` (the one search fuzzy-match tier that wasn't index-backed), `sessionEntities.entityId`, `sessionLocations.entityId`, `sessionPlotThreads.plotThreadId`, `campaignMembers.userId`, `timelineEventTags.tagId`, `entityTags.tagId`. Pure additive `CREATE INDEX` statements, no drops/renames/data-loss risk — reviewed before applying and verified directly against Postgres via `psql \d`.
- **`campaignMembers.userId` is the highest-value addition beyond the original audit's list**: it backs `CampaignsService.list(userId)` — the campaigns-list-for-this-user query, hit on every login — which the existing `(campaignId, userId)` unique constraint couldn't serve at all, since `campaignId` is unbound in that query.
- Fixed the confirmed N+1 in `CampaignsService.list()`: cover images for a whole campaign list were resolved with one `attachments` query per campaign; `resolveCoverImageUrls()` now batches them into a single `inArray` query. Added a real regression test that spies on the shared `pg.Pool`'s `.query` to count actual SQL round trips — verified it genuinely fails without the fix (2 vs. 4 queries going from one to three cover-image campaigns) before confirming it passes with the fix in place.
- Full suite re-run clean: 189 API integration tests, 92 unit tests, typecheck, lint.
- **Scope note**: this is Phase 6 of 13. Bundle analysis, load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.5] - 2026-07-15

### Added

- **Milestone 14, Phase 5 — Rate-limit tuning.** Rate limiting existed but was applied manually per-call-site, covering only `register`/`login`/`resendVerification`/`forgotPassword` and `inviteMember` — five auth endpoints (`verify-email`, `refresh`, `logout`, `reset-password`, `change-password`) and every non-auth module (entities, sessions, maps, attachments, plot-threads, timeline, exports, imports, campaigns CRUD) had no request-volume control at all.
- Added rate limits to all five previously-unprotected auth endpoints. `verifyEmailPerIp`/`resetPasswordPerIp` are tuned close to the existing login/forgot-password strictness — both accept an opaque-token guess from an unauthenticated caller, the same shape as credential guessing (`reset-password` in particular had zero throttle on that guess before this). `refreshPerIp`/`logoutPerIp`/`changePasswordPerIp` are generous, since refresh especially is a normal part of every page load. Threaded a `RequestContext` into `verifyEmail`/`resetPassword`/`logout`/`changePassword` (only `refresh` already took one).
- Added a global per-IP floor (`GlobalRateLimitGuard`, registered as `APP_GUARD`) covering every route with no specific limit. 300 requests/60s — calibrated against this repo's own heaviest e2e spec file (42 requests from the same loopback IP) with comfortable margin, not a real-world traffic estimate; a threshold worth revisiting once real usage exists. Auth's own tighter per-endpoint limits still trip first on their routes — this is a floor for everything else, not a replacement.
- 9 new tests (4 unit for the guard, 5 integration — one per newly-limited endpoint, following the existing "hammer until 429" pattern already used for login). Full suite re-run clean: 188 API integration tests, 92 unit tests, typecheck, lint.
- **Scope note**: this is Phase 5 of 13. Database indexing, bundle analysis, load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.4] - 2026-07-15

### Added

- **Milestone 14, Phase 4 — CORS and CSP hardening, and Phase 4b — Cookie and secrets review.** Both scoped hosting-agnostic and environment-driven per the user's explicit "feature-complete before Railway" direction — no Railway/R2/Sentry domains exist yet to hardcode.
- `main.ts`'s CORS config was a boolean (`origin: NODE_ENV === 'development'`) — permissive in dev, disabled entirely otherwise, with no way to actually allow a real deployed frontend. Added `CORS_ORIGIN` (comma-separated allow-list, parsed to an array in `packages/config`): empty in development still reflects any origin; empty anywhere else now fails closed until real origins are configured, replacing "disabled entirely" with an actually-usable default.
- Added `helmet` (secure defaults: CSP, `X-Content-Type-Options`, `X-Frame-Options`, HSTS, etc.), `crossOriginResourcePolicy` relaxed to `cross-origin` since the frontend is served from a different origin. CSP directives use helmet's own defaults rather than hand-tuned storage/monitoring domains that don't exist yet — revisit once R2/Sentry are real (Phases 9/11).
- **Caught a real architectural gap while implementing**: both `helmet()` and `cors()` were first added to `main.ts`'s `bootstrap()`, which — per this repo's own established `cookie-parser` precedent — Nest's testing module never runs, so the integration suite would have silently never exercised either. Moved both into `AppModule.configure()` alongside `cookie-parser`, keeping `main.ts` down to just `app.listen()`.
- **Cookie flags verified already correct** (ADR-0007): `httpOnly`, environment-conditional `secure`, `sameSite: 'lax'`, path-scoped, env-configurable `domain` — no fix needed, confirmed rather than assumed.
- **Secrets-validation gap closed**: added a `superRefine` to `apiEnvSchema` rejecting three known local-dev-only values (the `.env.example` JWT placeholder, MinIO's default storage credentials) outside `development` **and** `test` — `test` is exempted alongside `development` because CI's integration-tests job runs with `NODE_ENV=test` against ephemeral containers using these exact dev-shaped credentials. A deploy that copies `.env.example` without generating real secrets now fails to boot instead of silently running with well-known values.
- 5 new integration tests (`security-headers.e2e-spec.ts`, using `overrideProvider(EnvService)` to test multiple `NODE_ENV`/`CORS_ORIGIN` combinations without depending on ambient `.env` state) and 8 new unit tests (merged into the existing `packages/config/src/env.test.ts` after noticing a near-duplicate `.spec.ts` file was about to be created alongside it — consolidated instead). Full suite re-run clean: 183 API integration tests, 89 unit tests, typecheck, lint. `pnpm audit` unchanged (5 findings, all pre-existing dev-tooling-only) — `cors`/`helmet` introduced nothing new.
- **Scope note**: this is Phase 4 (+4b) of 13. Rate-limit tuning, database indexing, bundle analysis, load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.3] - 2026-07-15

### Added

- **Milestone 14, Phase 3 — Dependency upgrade: drizzle-orm.** The highest-blast-radius dependency change in this milestone — used in every database query across `apps/api` — done as its own dedicated, gated pass per the user's explicit process. Bumped `drizzle-orm` `^0.36.0` → `^0.45.2` (past the SQL-injection fix) and `drizzle-kit` `^0.28.0` → `^0.31.10` together, since generation and runtime need to stay compatible.
- **Process followed exactly as specified**: (1) full `pg_dump` snapshot of local Postgres taken first (249 TOC entries, verified restorable); (2) migration-drift review — `drizzle-kit generate` against a placeholder `DATABASE_URL`, mirroring CI's `validate-migrations` job — produced zero diff; (3) full API/worker suite — 178 integration tests, 89 unit tests, `apps/worker`'s 31 unit tests, and its real-infrastructure integration tier (4 tests against real Postgres + MinIO); (4) export/import round-trip test — both the automated `round-trip.integration.spec.ts`/`rollback.integration.spec.ts` tier and a **live** manual round trip through the actual running app; (5) representative manual CRUD checks, folded into that same live session (create/edit-via-autosave/delete on a real campaign).
- **Found and fixed one real regression along the way**: `relationship-types.service.ts`'s `isUniqueViolation()` checked `error.code === '23505'` directly, but drizzle-orm ≥0.4x now wraps every query error in a `DrizzleQueryError` with the real driver error nested on `.cause` — a duplicate relationship-type key started 500ing instead of correctly 409ing. Fixed to check both shapes. Repo-wide grep confirmed this was the only place inspecting a raw Postgres error code.
- **Live round-trip verification** (Playwright-driven, real dev stack): created a campaign, created and edited an entity (confirming autosave persists correctly through the upgraded ORM), created a session and plot thread, soft-deleted a second entity, exported the campaign, downloaded the real archive, re-uploaded it through the import UI, confirmed the dry-run report's counts, confirmed the import, and verified the new campaign has the edited entity under its _edited_ name while correctly excluding the soft-deleted one.
- `pnpm audit` no longer lists `drizzle-orm` (or `nodemailer`, from Phase 2) at all. Remaining findings are the pre-existing dev-tooling-only `esbuild`/`vite`/`vitest` vulnerabilities already noted as lower-priority in Phase 4's audit — not addressed in this phase, since they weren't part of the user's explicit gate.
- **Per the user's explicit gate, both required dependency upgrades (nodemailer, drizzle-orm) are now complete and clean.** No live Railway provisioning happens regardless until the app is feature-complete (deferred to Milestone 16), but this specific blocker is cleared.
- **Scope note**: this is Phase 3 of 13. CORS/CSP/cookies/secrets hardening, rate-limit tuning, database indexing, bundle analysis, load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.2] - 2026-07-15

### Added

- **Milestone 14, Phase 2 — Dependency upgrade: nodemailer.** `pnpm audit` had flagged `nodemailer` (pinned `^6.9.16`) as many majors behind the patched line — unpatched SSRF, arbitrary-file-read, and injection CVEs. Bumped to `^9.0.3` (latest) plus `@types/nodemailer@^8.0.1`. `mail.service.ts`'s usage — `createTransport`/`sendMail` with a plain SMTP config — is a small, stable surface unaffected across nodemailer's major versions; no code changes were needed. `pnpm audit` no longer lists nodemailer.
- Validated all three real email workflows end to end against real Mailpit (not mocked), per the explicit user-directed process: registration verification, password reset, and campaign invitations — all exercised by the existing e2e suite's Mailpit-polling helpers, all pass. Full suite re-run clean: 178 integration tests, 89 unit tests, typecheck, lint.
- **Scope note**: this is Phase 2 of 13. Per the user's explicit gate, drizzle-orm's upgrade (Phase 3 — dedicated pass: DB snapshot, migration-drift review, full suite, export/import round-trip, manual CRUD checks) is next, and no live Railway provisioning happens until both dependency upgrades are complete and clean.

## [0.14.1] - 2026-07-15

### Added

- **Milestone 14, Phase 1 — Authorization audit.** A pre-implementation audit (three parallel research passes: security/auth, performance, reliability/ops — see `WORLDBINDER_V1_ROADMAP.md`'s Milestone 14 phase breakdown) found the authorization model itself in solid shape: every spot-checked controller (entities, sessions, maps, attachments, membership, exports) correctly layers a coarse route-level `CampaignRolesGuard` with fine-grained `CampaignPolicyService` checks, and cross-campaign isolation holds everywhere checked.
- Two real fixes: `CampaignsController.update()` (`PATCH /campaigns/:campaignId`) was the one mutating endpoint missing the route-level `@RequireCampaignRole` guard every sibling endpoint has — its permission logic is genuinely field-dependent (renaming is owner-only, other settings are owner/gm, which no single static guard can fully express), so it relied on a service-level check alone. Added `@RequireCampaignRole('owner', 'gm')` as a coarse floor — purely additive, changes no actual behavior since owner/gm already pass the existing fine-grained checks, but means a future field added without its own policy check still can't be reached by editor/player/viewer. `AttachmentsService.complete()` and `delete()` both wrote their final `UPDATE` filtering only by `attachments.id`, not `campaignId` — unlike entities/sessions/maps' equivalent writes, which re-assert `campaignId` directly in the mutating query rather than relying solely on an earlier read. Both now include the check directly in the `WHERE`.
- **One audit finding turned out to be wrong, and is documented as such rather than silently dropped**: the initial pass flagged `CampaignPolicyService.canExportCampaign` as dead code. It isn't — `exports.service.ts`'s `assertCanExport()` calls it, correctly layered under `ExportsController`'s own route guard, the same two-layer pattern as everywhere else. Verified via direct grep before assuming the finding was real.
- New `docs/security/threat-model.md`: trust boundaries, the auth model (ADR-0007) and two-tier visibility model (ADR-0009) as implemented today, the Railway/R2/Sentry/email-provider topology decided for this milestone (not yet provisioned — see below), and every gap found across all of Milestone 14's phases as known risks in one place.
- Full suite re-run clean: 178 API integration tests, 89 unit tests, typecheck, lint. **Caught a false regression signal while verifying**: 4 attachments tests initially failed when run after the campaigns suite, traced to a leftover `pnpm dev` stack (API/worker/web) from earlier work in this session still running against the same Postgres/Redis the integration tests use — the real worker was racing the tests' own direct-DB `markReady()` simulation. Killed the stray processes and re-ran clean; not a real regression, but a reminder to fully tear down dev stacks between sessions of work.
- **Hosting decision recorded (2026-07-15, user-directed)**: the intended future deployment target is Railway (API and worker as separate services, Railway-managed Postgres/Redis), Cloudflare R2 replacing MinIO, a transactional email provider (Resend or Postmark) replacing raw SMTP, and Sentry for error monitoring. Explicitly **not provisioned this milestone** — the app is to be feature-complete first. Milestone 14 makes every hosting-dependent integration point environment-driven and swappable; actual provisioning, live backup drills, live alert testing, and production smoke tests are deferred to Milestone 16 (v1.0 Release Candidate), noted there as a forward reference.
- **Scope note**: this is Phase 1 of 13. Dependency upgrades (nodemailer, then drizzle-orm in a dedicated gated pass), CORS/CSP/cookies/secrets hardening, rate-limit tuning, database indexing, bundle analysis, load tests, and the storage/email/monitoring/backup/runbook work remain, tracked in the roadmap.

## [0.14.0] - 2026-07-15

### Added

- **Milestone 13 — UX and Accessibility Hardening, complete (Phase 8 of 8 — Regression-proofing).** Added `eslint-plugin-jsx-a11y` to `packages/eslint-config/react.js` (the shared config `apps/web` extends). First run surfaced 5 findings: 4 were legitimate patterns fixed with a targeted `eslint-disable` and inline justification rather than code changes (`MapCanvas`'s click-to-place background div, whose keyboard equivalent is Phase 2's "+ New pin" button; `SearchOverlay`'s backdrop click-to-dismiss, whose keyboard equivalent is Escape; and its `role="dialog"` element's own `onKeyDown`, a standard modal pattern jsx-a11y's interactive-role allowlist doesn't recognize) — and one was a real fix: a redundant explicit `role="list"` on a real `<ul>` in both `EntityMultiPicker` and `TagInput` (added in Phase 2), removed since `<ul>` already conveys list semantics natively.
- 7 new `getByRole`/`getByLabelText` tests on the three riskiest widgets touched in Phase 2: `MapPinMarker.test.tsx` (Enter/Space activation — the confirmed keyboard bug — and that unrelated keys don't), `Combobox.test.tsx` (`aria-activedescendant` tracks the active option as arrow keys move, Enter selects it), `TagInput.test.tsx` (chips render as a real list, add/remove both announce via the live region).
- **Caught and fixed a real, latent test-infra bug while writing them**: `apps/web/src/test/setup.ts` never wired up testing-library's automatic cleanup, and since `vite.config.ts` sets `test.globals: false`, the library's default `afterEach`-based auto-cleanup never actually registered — any test file with more than one `render()` call silently leaked DOM into subsequent tests in the same file (confirmed: all three new multi-test files failed with "multiple elements found" until fixed). Added an explicit `afterEach(cleanup)` to the shared setup file.
- Full suite re-run clean: typecheck/lint/tests across `apps/web`, `packages/ui`, and `apps/api` (the last to confirm the shared eslint-config change didn't cross-contaminate — it doesn't; `apps/api` has its own independent config).
- **Milestone-level status**: all 8 phases done (0.13.1–0.14.0). Tablet-layout and onboarding exit criteria are directly addressed and manually verified. The WCAG 2.2 AA criterion is addressed for every concrete gap this milestone's own audit found (contrast computed against the real WCAG formula, keyboard/focus-trap/`aria-activedescendant` fixes, static a11y linting) but this was not a formal compliance audit — no automated `axe` scan, no manual testing with real assistive technology (NVDA/JAWS/VoiceOver). Treat as "hardened against the issues found," not "certified compliant" — see the roadmap's Milestone 13 status note for the honest version of this claim.
- **Scope notes across all of Milestone 13**: `packages/ui` still has no lint step of its own (its components are only linted where consumed by `apps/web`) — a pre-existing gap, not introduced here, and a reasonable candidate for a future milestone. No `jest-axe`/`@axe-core/react` runtime scanning was added. Reduced-motion (Phase 5) required no code change — the only motion in the app already had a guard from Phase 1.

## [0.13.6] - 2026-07-15

### Added

- **Milestone 13, Phase 7 — Browser compatibility pass.** `playwright.config.ts` previously configured only `chromium` — its own comment already anticipated Firefox/WebKit landing here. Added both (`devices['Desktop Firefox']`/`devices['Desktop Safari']`), the repo's now-documented supported-desktop-browser list (see the roadmap's Milestone 13 Phase 7 entry). No mobile-emulation project — desktop/tablet is this milestone's scope (Phase 4), not phone.
- **Ran the full 7-spec e2e suite against both new browsers and found 5 real failures — all test-locator bugs, not product bugs, confirmed by a clean chromium re-run of the same specs after fixing them.** `sessions.spec.ts` and `plot-threads.spec.ts` both asserted a bare `getByText('completed')` right after completing a session; the async-loading Revision History panel's diff text can also contain the word "completed," and once that panel finishes loading the locator becomes ambiguous — a timing race that chromium happened not to lose but firefox/webkit did. Fixed by scoping both to `.wb-entity-header__meta` (the actual status element) instead. `search.spec.ts` pressed the Ctrl/Cmd+K shortcut immediately after `page.goto()`; `goto()` resolves at the load event, not after React has mounted `CampaignLayout`'s `keydown` listener, so the keypress could fire into a page that wasn't listening yet — fixed by waiting for a real post-mount element (the "World" nav link) first.
- Full 3-browser e2e suite (19 previously-passing tests re-confirmed on firefox/webkit, the 3 fixed specs re-run clean on all of chromium/firefox/webkit) plus the existing unit/typecheck/lint suites all pass.
- **Scope note**: this is Phase 7 of 8. Only Phase 8 (regression-proofing via `eslint-plugin-jsx-a11y` and accessibility-aware tests) remains.

## [0.13.5] - 2026-07-15

### Added

- **Milestone 13, Phase 6 — Onboarding and help content.** No tooltip/help/walkthrough/first-run component existed anywhere before this; the only "onboarding" surface was one plain sentence on a zero-campaign list.
- New `apps/web/src/features/help/pages/HelpPage.tsx` at a public `/help` route (accessible whether or not the visitor is registered, on purpose): a getting-started checklist, a "where things live" map of every nav item to what it's for, an explanation of public/GM-only visibility, and a role reference (Owner/GM/Editor/Player/Viewer, copied accurately from roadmap §5, not paraphrased from memory). Deliberately static content, not a guided-tour library — one page of real content doesn't justify building tour infrastructure, consistent with this codebase's "build up only as real screens need them" philosophy.
- `CampaignsListPage`'s zero-campaign `EmptyState` now uses the `action` slot (built in Phase 1, previously unused anywhere) to link to the new Help page.
- **Caught and fixed a real, visible pre-existing bug while wiring up the new Help link**: `App.tsx`'s header nav (`.app-shell__nav`) had zero CSS at all, so its `<Link>`s rendered directly adjacent with no separating whitespace — "CampaignsAccount" concatenated, confirmed in Phase 1's own verification screenshots but not flagged as in-scope at the time. Adding a third "Help" link would have compounded it to "CampaignsAccountHelp", so fixed it now: `.app-shell__nav { display: flex; flex-wrap: wrap; gap: 1rem }`.
- **Also fixed while in the area**: `MembersPage` still had the pre-shared-component ad hoc `<p>Loading members…</p>` pattern from before Phase 1 (missed in that pass since it wasn't in the original list-page sweep) — now uses `LoadingState`/`ErrorState`/`EmptyState` like every other list page.
- Full existing suite re-run clean (typecheck, lint, existing web test). Manually verified end to end (Playwright-driven): confirmed `/help` renders both logged-out and logged-in, confirmed the header nav now shows visibly spaced "Campaigns / Account / Help" instead of concatenated text, and read through the rendered Help page content for accuracy against the actual nav labels and roadmap's role definitions. No new console errors.
- **Scope note**: this is Phase 6 of 8. The exit criterion "new users can complete onboarding without external help" is addressed by the Help page's content, not validated with a real first-time user — no such user was available to test with in this session. Phase 5 (reduced-motion) was confirmed already done with no code change: the only motion anywhere in the app is `LoadingState`'s spinner from 0.13.1, which already carries a `prefers-reduced-motion` guard. Browser-compatibility pass and regression-proofing (`eslint-plugin-jsx-a11y` + tests) remain, tracked in the roadmap.

## [0.13.4] - 2026-07-15

### Added

- **Milestone 13, Phase 4 — Responsive/tablet layout.** Before this, `global.css` had exactly one `@media` query in the whole app (`prefers-color-scheme: dark`) — zero viewport breakpoints, and several `display: flex` rows with no `flex-wrap`. Most of the app turned out to already be responsive by construction (fixed `max-width`s on block elements shrink naturally with no explicit `width`; `.wb-map-list` already uses CSS Grid `auto-fill`; map pins are positioned by normalized percentage, not pixels) — the real, concrete defect was the handful of unwrapped flex rows that would force horizontal page scroll below a certain width.
- **The one confirmed blocking defect**: `CampaignLayout`'s main nav (`.wb-links`, 8 items — Dashboard/World/Sessions/Threads/Maps/Search/Members/Settings) was a `display: flex` row with no wrap, the same class also used by `AccountLayout`'s nav and auth-page footer links. Added `flex-wrap: wrap` (plus a two-axis `gap` so wrapped rows don't touch). Also added `flex-wrap: wrap` to six other flex rows that carried the same latent risk at smaller sizes or longer content, none individually confirmed overflowing at the tested widths but all safety nets with no downside at wide viewports: `.app-shell__header`, `.wb-session-list li`, `.wb-combobox__option`, `.wb-pagination`, `.wb-map-pin-list li`, `.wb-map-layer-manager li`.
- **Picked and documented a tablet target**: 768px viewport width (iPad-portrait class), noted in a `global.css` comment above `.wb-links` — the roadmap's own "desktop/tablet" scope, no phone-width (<600px) target implied or tested for further than a sanity check.
- Full existing suite re-run clean (typecheck, lint, existing web test — this phase touched only CSS). Manually verified via Playwright viewport resizing: `document.documentElement.scrollWidth` vs `clientWidth` (the actual mechanism behind "no horizontal scroll") checked with zero overflow at 768×1024, 1024×768, and 600×900 on the campaign dashboard, world list, and session list; additionally checked 380×900 (well past the tablet scope) to prove the nav-wrap fix does real work, not just pass by coincidence — confirmed the nav visibly wraps to two lines there with still no page-level horizontal scroll.
- **Scope note**: this is Phase 4 of 8. No new breakpoint-gated CSS was needed beyond the `flex-wrap` fixes — everything else already scaled without a media query, so no dedicated "tablet stylesheet" was built (would have been complexity with no defect to justify it). Visual density at tablet width (e.g. `.wb-related-content`'s dashboard sections, which are single-column even on desktop) is unchanged — a design opportunity, not a defect, and out of scope here. Reduced-motion, onboarding/help content, browser-compatibility pass, and regression-proofing remain, tracked in the roadmap.

## [0.13.3] - 2026-07-15

### Added

- **Milestone 13, Phase 3 — Contrast review.** Fixes the concrete contrast gaps the audit found in `global.css` (see `WORLDBINDER_V1_ROADMAP.md`'s Milestone 13 phase breakdown), computed against WCAG's actual relative-luminance formula rather than eyeballed.
- **Muted/secondary text was done via CSS `opacity` on `--wb-fg`, which stacks unpredictably with theme and background** — `wb-combobox__meta` at `opacity: 0.6` was the worst offender and almost certainly failed AA's 4.5:1 for normal text. Replaced every text-dimming `opacity` rule (`status-panel dt`, `wb-session-list__meta`, `wb-combobox__meta`/`__status`, `wb-dropzone p`, `wb-search-overlay__status`, `wb-search-result__subtitle`/`__snippet`) with a new solid `--wb-muted-fg` token — `#4b5563` light (~6.9:1 against both `--wb-bg` and `--wb-muted-bg`), `#9ca3af` dark (~6.4:1). `opacity` is left alone on the two places it's legitimate (`:disabled` states, which WCAG exempts from contrast minimums) and the visually-hidden file input.
- **`--wb-border` bumped from ~1.2:1 to ~3.4:1 (light) / ~3.3:1 (dark) against page background**, meeting WCAG 1.4.11's 3:1 non-text-contrast minimum for UI component boundaries — this token backs the visible edge of every field input, listbox, dropzone, and empty/error-state box in the app (20 usages), so the old near-invisible value meant most component boundaries in the app were essentially imperceptible to low-vision users.
- **`.wb-banner`/`.wb-banner--warning` were referenced in `EntityFormPage.tsx` (the draft-restored notice and the edit-conflict warning) but never defined in `global.css` at all** — rendered completely unstyled. Added both, plus a new `--wb-warning` token (`#b45309` light ~5.0:1, `#fbbf24` dark ~10.7:1) — only error/success had tokens before this.
- Full existing suite re-run clean (typecheck, lint, existing web test). Manually verified end to end against the running dev stack in both light and dark color schemes (Playwright-driven, `colorScheme` context option): confirmed empty-state/field/dropzone borders are now clearly visible in both themes, confirmed no layout regression on entity detail, world list, and the search overlay's combobox meta/status text. No new console errors.
- **Scope note**: this is Phase 3 of 8. The remaining phases (responsive/tablet layout, reduced-motion, onboarding/help content, browser-compatibility pass, regression-proofing via `eslint-plugin-jsx-a11y`) are tracked in the roadmap, not yet done. Contrast ratios were computed by hand against the WCAG relative-luminance formula, not measured with automated tooling (no contrast-checking tool is wired into this repo yet — a natural candidate for the Phase 8 regression-proofing pass).

## [0.13.2] - 2026-07-15

### Added

- **Milestone 13, Phase 2 — Keyboard and screen-reader fixes.** Closes the concrete gaps the pre-implementation audit found (see `WORLDBINDER_V1_ROADMAP.md`'s Milestone 13 phase breakdown), not just restyling.
- **`MapPinMarker` keyboard-activation bug fixed**: a real, focusable `<button aria-label>` on the map canvas was wired only to `onPointerDown`/`onPointerMove`/`onPointerUp` — Enter/Space (which dispatch a native `click`, not pointer events) silently did nothing. Added a dedicated `onActivate` prop, invoked from a new `onKeyDown` handler, wired through `MapCanvas` to the same `onPinActivate` callback pointer clicks already use. Canvas pins are now keyboard-operable directly, not only via the separate `AccessiblePinList`.
- **Map pin reposition has a keyboard equivalent for the first time**: dragging a pin was the only way to move it. `MapPinForm` gained "Position — left (%)"/"Position — top (%)" number fields (`MapPinFormValues.xNormalized`/`yNormalized`), pre-filled from the pin's current position when editing or the clicked/default position when placing; `MapDetailPage.handlePinFormSubmit` now calls the existing narrow `useRepositionMapPinMutation` alongside the regular update when the form's position differs from the pin's stored one. A new "+ New pin" button (manage mode) opens the placement form at a centered default position, so placing a pin no longer strictly requires clicking the canvas either.
- **`SearchOverlay` focus trap and restore**: it declared `role="dialog" aria-modal="true"` but implemented neither — Tab could escape to the page behind the backdrop, and closing never returned focus to whatever triggered Ctrl/Cmd+K. Added a minimal `Tab`-wrapping focus trap scoped to the panel (no shared modal primitive exists in `packages/ui` yet — still true as of this milestone) and a `previouslyFocusedRef` that restores focus on close.
- **`aria-activedescendant` added to `SearchOverlay` and `packages/ui`'s `Combobox`**: both implement `role="combobox"`/`listbox`/`option` but never told screen readers which option arrow keys had moved to. Each option now has a stable id and the input's `aria-activedescendant` tracks the active index.
- **Visible focus indicator restored**: `.wb-search-overlay__input:focus-visible { outline: none }` removed the keyboard focus ring with no replacement (WCAG 2.4.7) — now outlines with `--wb-accent`, matching every other focusable field in the app.
- **Silent chip add/remove fixed**: `TagInput` and `EntityMultiPicker`'s chip rows had no `role="list"` and no live-region announcement, so adding or removing a tag/entity was invisible to screen-reader users. Both chip containers are now `<ul role="list">`/`<li>` (was `<span>`/`<div>`) with a `wb-visually-hidden` `role="status" aria-live="polite"` region announcing each change. New `.wb-visually-hidden` utility class in `global.css` (the app's first screen-reader-only utility).
- Full existing suite re-run clean (typecheck, lint, existing web test). Manually verified end to end against the running dev stack (Playwright-driven): confirmed `aria-activedescendant` updates as arrow keys move through search results, confirmed Escape closes the search dialog, confirmed a canvas pin focused via Tab and activated via Enter opens the edit form (previously did nothing), confirmed the new position fields are pre-filled with the pin's real coordinates (30/40 on a test pin) and are independently editable, confirmed the "+ New pin" button opens the placement form, and confirmed the search input now shows a visible focus ring. No new console errors.
- **Scope note**: this is Phase 2 of 8. `MapCanvas`'s pointer-drag reposition itself remains pointer-only by design (the new form fields are the keyboard path, not a replacement); the still-outstanding phases (contrast, responsive/tablet layout, reduced-motion, onboarding/help, browser-compatibility pass, regression-proofing via `eslint-plugin-jsx-a11y`) are tracked in the roadmap, not yet done.

## [0.13.1] - 2026-07-14

### Added

- **Milestone 13, Phase 1 — Loading/error/empty-state foundations.** A pre-implementation audit (now recorded in `WORLDBINDER_V1_ROADMAP.md`'s Milestone 13 section, broken into 8 phases) found this milestone genuinely greenfield: no shared loading/error/empty-state components anywhere, and real gaps hiding behind the ad-hoc per-page pattern. This phase builds the shared components and fixes the concrete bugs the audit found.
- New `packages/ui`: `LoadingState` (spinner + text, `role="status" aria-live="polite"`, spinner respects `prefers-reduced-motion`), `ErrorState` (`role="alert"`, optional `onRetry` wired to the query's `refetch()`), `EmptyState` (message + optional action). Corresponding `global.css` rules (`.wb-loading-state`/`.wb-error-state`/`.wb-empty-state`, plus the `@keyframes wb-spin` and its reduced-motion guard — the first reduced-motion handling anywhere in the app).
- Rolled out across every list page (`WorldListPage`, `SessionListPage`, `MapListPage`, `TimelineListPage`, `ThreadListPage`, `CampaignsListPage`) and the shared detail-page sub-panels (`AttachmentsPanel`, `RelatedContentPanel`, `RevisionHistoryPanel`), replacing inline `<p>Loading…</p>`/bare `<FormMessage>`/bare `<li>No X yet.</li>` JSX and the loading-guard drift between them (some pages guarded the empty check with `!isLoading`, others didn't).
- **Concrete bugs fixed, not just restyled**: `CampaignOverviewPage`'s dashboard query had no loading or error handling at all — a slow or failed fetch rendered indistinguishable blank/"—" placeholders; `EntityDetailPage` collapsed every query error into "This entry could not be found," misreporting real 500s/network errors as 404s (now checks `ApiError.status === 404` specifically via `apps/web/src/lib/apiClient.ts`'s `ApiError`, only showing the not-found message for an actual 404); `ProfilePage` rendered `null` with zero loading affordance while the auth store's user was still resolving; `SessionsPage` had no empty-state message path at all.
- Full existing suite re-run clean (typecheck, lint, and the existing web test file all pass); manually verified end to end against the running dev stack (Playwright-driven): logged in as the demo user, walked the campaigns list, campaign dashboard, World/Sessions/Maps/Threads list pages, and an entity detail page (whose Relationships/Backlinks/Attachments/Revision-History panels all render the new `EmptyState` consistently), confirmed no layout breakage and no new console errors.
- **Scope note**: this is Phase 1 of 8 for Milestone 13 (see roadmap). Responsive/tablet layout, keyboard/screen-reader fixes (including the confirmed `MapPinMarker` keyboard-activation bug), contrast, onboarding/help content, and the browser-compatibility pass are tracked as separate phases, not yet done.

## [0.13.0] - 2026-07-14

### Added

- **Milestone 12 — Export and Import.** A versioned zip archive format for a full campaign (entities, relationships, wiki-links, sessions, plot threads, maps, timeline, tags, and attachments), a background export job with a status-polling UI, an upload → validate → dry-run-report → confirm → transactional-import pipeline for restoring an archive as a brand-new campaign, and checksum/whitelist/content-re-verification defenses against malicious archives — both API, worker, and web UI.
- New schema: `campaign_exports` (`status` pending/processing/ready/failed, opaque `storage_key`, mirrors `attachments`' shape) and `campaign_imports` (`status` pending/validating/dry_run_ready/importing/completed/failed, `dry_run_report_json`/`import_report_json`, `result_campaign_id` set only on completion — not campaign-scoped, since importing _creates_ a campaign, matching the roadmap's global `POST /imports` route). `campaign_audit_event_type` gains `campaign_exported`/`campaign_imported` (roadmap §11.14 requires both).
- The roadmap's §17 archive format only specifies file layout, not a job-tracking schema — same "necessary but undocumented" gap Milestone 11's calendar config filled. One deviation from §17.1's literal file list, documented: an `attachments.json` metadata file alongside the `attachments/` folder of raw bytes, since the literal list has nowhere to put attachment metadata. **Key research finding that simplified the design**: `entityWikiLinks.sourceResourceType` is only ever `'entity'` today, so wiki-link import needs only the entity id map — no cross-dependency on sessions/plot-threads. Built-in relationship types have fixed ids seeded identically in every database (`built-in-relationship-types.ts`) and are never exported or remapped; only custom per-campaign types are, matched by their unique `key`.
- **Security validation (`apps/worker/src/imports/archive.ts`'s `openArchive()`)** is the concrete mechanism behind "malicious archives are rejected": archive size capped before download even starts (`HeadObjectCommand` first), entry count capped, and — the single strongest defense — every entry name is checked against an exact whitelist of the known top-level files plus `attachments/<uuid>`, rejecting anything else outright. This one check covers path traversal and symlinks in one pass, deliberately simpler and stronger than trying to sanitize arbitrary names or inspect zip symlink bits. Declared entry size is checked before decompression and the _actual_ decompressed size is re-checked after (defense in depth against a lying header, the classic zip-bomb shape, since `adm-zip` buffers the whole archive in memory). Every file's sha256 is recomputed and compared against `checksums.json`. Every JSON document is Zod-validated against schemas in `packages/validation/src/import-export.ts`. Every bundled attachment's _actual bytes_ are re-run through the existing `sniffMimeType()`/`looksLikeText()` — `attachments.json`'s declared mime type is never trusted; a mismatch drops that one attachment (warning) rather than failing the whole archive.
- **Transactional import (`apps/worker/src/imports/run-import.ts`)** is one Postgres transaction end to end, with dependency-driven insertion order (campaign → tags → custom relationship types → entities → relationships → sessions → plot threads → their join tables → wiki-links → maps → timeline events → attachments → resource-attachment links), remapping every old id to a freshly generated new one via per-resource `Map`s built as each table is written. Any failure anywhere rolls back everything — the concrete mechanism behind "failed import leaves no partial data," not a manually-written cleanup path. Search vectors for entities/sessions/plot-threads/timeline-events are rebuilt inline with a follow-up `UPDATE` per row (raw SQL, not Drizzle — `apps/worker` is a plain process, not a NestJS app with DI, so `buildWeightedTsvector`'s Drizzle-tagged-template return type isn't usable here).
- `apps/api/src/jobs/`: `ExportQueueService`/`ImportQueueService`, exact mirrors of Milestone 9's `AttachmentQueueService` — its own doc comment already anticipated hosting this. `apps/api/src/exports/` (`POST/GET /campaigns/:campaignId/exports`, owner/gm only — roadmap's permission matrix marks Editor "Configurable" for exporting, same default-to-off precedent as `canEditEntities`'s Player row) and `apps/api/src/imports/` (`POST /imports/presign` → `POST /imports/:id/complete` → `GET /imports/:id` → `POST /imports/:id/confirm`, mirroring attachments' presign→complete flow exactly, extended with a confirm step; not campaign-scoped, so every method scopes by the requesting user's id instead of a membership guard — "you can only see/act on your own import jobs").
- Frontend: `features/exports/` (`ExportsPage` at `/app/campaign/:campaignId/import-export`, linked from Settings — trigger + polling history list, freshly presigned download link once ready) and `features/imports/` (`ImportCampaignPage` at `/app/campaigns/import`, linked from the campaigns list — upload via the existing `FileDropzone`/`uploadToStorage` pipeline, polls through validating → dry-run report with counts/warnings → confirm → importing → completed, with a link straight to the new campaign).
- 10 new unit tests (`validateImport` rejection cases — path traversal, unwhitelisted entry, entry-count cap, tampered checksum, missing checksum, bad schema version, bad format, non-zip bytes, plus the content-detection-drops-a-mismatched-attachment case — built against real in-memory zips via `adm-zip` in the test itself). A new `apps/worker` real-infrastructure test tier (`*.integration.spec.ts`, its own `test:integration` script, excluded from the plain `vitest run` used in CI) distinct from the existing fake-pool/fake-s3 unit tests: **the literal exit-criterion test** seeds a representative campaign (entities with relationships/tags, a session, a plot thread, a map with a pin, a timeline event, a real attachment through MinIO) directly via SQL, calls `exportCampaign()`/`validateImport()`/`runImport()` against real Postgres + real MinIO, and asserts the resulting new campaign's data is equivalent with fresh ids; a second test forces a genuine FK-constraint failure partway through `runImport()` (a bogus relationship-type id) and asserts zero new campaign rows survive the rollback. 15 new `apps/api` integration tests (`exports.e2e-spec.ts`, `imports.e2e-spec.ts`) covering the API surface (permissions, cross-campaign isolation, ownership scoping, status transitions, double-complete/early-confirm rejection) against a simulated worker-completed state, the same "simulate the worker's terminal state via a direct DB write" precedent `attachments.e2e-spec.ts` already established. Full existing suite (178 API integration tests, 89 unit tests) re-run clean.
- Manually verified end to end against a freshly restarted dev stack, worker included (Playwright-driven): created a campaign with a real entity, created an export and watched it reach `ready`, downloaded the actual generated zip and re-uploaded it through the import page, watched it validate to a dry-run report correctly showing "entities: 1", confirmed the import, and landed on a brand-new campaign with the same entity present under a new id. Caught and fixed a real bug in the process: two raw SQL queries (in both `export-campaign.ts` and `run-import.ts`) referenced the `relationship_types.symmetric` column unquoted — `SYMMETRIC` is a Postgres reserved word, so this passed typecheck but failed at query time; fixed by quoting the identifier. Also caught and fixed a Playwright selector bug in the verification script itself (clicking the `FileDropzone`'s label text instead of its actual `role="button"` drop target never opens the file picker) — not a product bug, but a reminder that a component's accessible click target isn't always its visible label.
- **Scope notes**: member roles/labels are exported for historical reference but not re-imported as live `campaign_members` rows (no email to resolve to a real user; only the importing user becomes a member, as owner). Session `participants` (which reference `campaign_members`) are dropped on import for the same reason. No revisions are created for freshly-imported resources, matching Maps having none either — an import is a restore, not an edit session. Archive/entry size caps and the entry-count cap (`packages/validation/src/import-export.ts`) are documented judgment calls in the spirit of `ATTACHMENT_MAX_SIZE_BYTES`. The "format migration layer" deliverable is a real, tested extension point (`migrateArchive()` in `archive.ts`) that's a no-op passthrough today, honestly, since only one schema version exists yet.

## [0.12.0] - 2026-07-14

### Added

- **Milestone 11 — Timeline and Calendar.** Per-campaign custom calendars (named months with configurable day counts), a calendar-aware structured date editor, timeline events with year/month/day precision plus an "approximate" (circa) flag and an undated section, entity/session/tag links, filters, and full-text search integration — both API and web UI.
- New schema: `campaigns.calendar_config_json` (nullable `jsonb`, one config per campaign — extends the existing row rather than a new table, mirroring `currentWorldDateJson`/`coverAttachmentId`; `null` means `DEFAULT_CALENDAR_CONFIG`, a standard non-leap Gregorian 12-month calendar, applies) and `timeline_events` (`date_precision` enum `year`/`month`/`day`, `start_date_json`/`end_date_json` carrying only the fields implied by that precision, single `visibility` column and single `search_vector` — no public/GM content split, since a timeline event has no GM-only sub-content, unlike entities/sessions). `timeline_events` hard-deletes with no revision history, matching Maps' precedent for this milestone's newly-added resource rather than entities/sessions/plot-threads' soft-delete treatment. Join tables `timeline_event_entities`/`timeline_event_sessions`/`timeline_event_tags` mirror `plot_thread_entities`/`entity_tags`'s composite-key shape.
- The roadmap left calendar configuration entirely unspecified (§9.13 only defines `timeline_events`) — the codebase already carried a seed for this milestone to build on: `worldDateSchema`'s existing comment ("Deliberately Gregorian-shaped... Milestone 11 owns real calendar settings") is now made real. `packages/validation/src/calendar.ts` gained `calendarConfigSchema`, `DEFAULT_CALENDAR_CONFIG`, `timelineDateSchema` (partial year/month/day + `approximate`), and pure functions `isValidTimelineDate`/`isValidWorldDate`/`compareTimelineDates`/`timelineDateToOrdinal` — the single source of truth for date validation and sorting, shared by the API, `CampaignsService`, `SessionsService`, and the web app's date-picker bounds.
- **`CampaignsService.update()`** now validates a calendar-config change against every date already stored under the campaign — its own `currentWorldDateJson`, every session's world start/end date, every timeline event's start/end date — via direct `sessions`/`timeline_events` table reads (no cross-module service import, same "sibling table read" precedent as the attachments cover-image check), rejecting with `409` if any would become invalid. This is the concrete mechanism behind the "existing dates remain interpretable after allowed calendar changes" exit criterion. `SessionsService.create()`/`update()` similarly validate `worldStartDateJson`/`worldEndDateJson` against the campaign's real calendar on write, replacing the previous hardcoded month-1-12/day-1-31 Zod bounds (now unbounded at the schema layer, real-validated at the service layer where campaign context is available).
- `apps/api/src/timeline/` (`TimelineModule`/`TimelineService`/`TimelineController`): CRUD at `/campaigns/:campaignId/timeline` with `entityId`/`sessionId`/`tag` list filters; visibility gating is the standard single-column 403→404 pattern (ADR-0008), not map pins' multi-way composition, since a timeline event has one visibility column, not layers. Sorting fetches the campaign-scoped, visibility-filtered set (bounded to the roadmap's 500-event perf target) and sorts in JS via `compareTimelineDates` — a custom calendar's variable month lengths make expressing the sort in SQL impractical, same "bounded query, sort in JS" precedent as `search.service.ts`. Undated events always sort last, ordered among themselves by creation time. `CampaignPolicyService.canManageTimeline` joins the existing `ENTITY_EDITOR_ROLES` actor-set convention.
- Search integration (roadmap §14.1 already lists "Timeline events" as searchable, unlike Maps, which isn't): `SearchResourceType` gained `'timeline_event'`, and `SearchService.searchTimelineEvents()` mirrors the existing per-resource tier/score ranking exactly, gated by row-level visibility only.
- Frontend `features/calendar/`: `StructuredDateEditor` (calendar-aware year/month/day + approximate/undated editor, reading month names and day bounds from the campaign's real config or the default) and `CalendarMonthsEditor` (add/remove/reorder month name+day-count rows — no generic list-editor primitive exists in `packages/ui` yet, built feature-local per its "built up only as real screens need them" philosophy). `SessionFormPage`'s previously ad-hoc plain-number world-date inputs now use `StructuredDateEditor`, making existing session world-dates calendar-aware too. `CampaignSettingsPage` gained a "Calendar" section using `CalendarMonthsEditor`. New `features/timeline/`: list page (chronological, Undated section, entity/tag filters), detail page, and form page (title/summary/`RichTextEditor` content, start/end `StructuredDateEditor`s, `EntityMultiPicker`, a new `SessionMultiPicker` mirroring `EntityMultiPicker`'s chip+combobox shape, tag input). "Timeline" linked from the World page header rather than a full World tab-nav redesign (out of scope for this milestone). `timeline_event` added to the search results page's type filter.
- 5 new unit tests (`isValidTimelineDate`/`isValidWorldDate`/`compareTimelineDates` against both the default and a custom fantasy calendar, including a >31-day month) and 1 new policy spec case (`canManageTimeline`). 13 new integration tests (`timeline.e2e-spec.ts`) covering CRUD, cross-campaign rejection, undated creation, out-of-range and custom-calendar date validation, end-before-start rejection, the calendar-change-invalidates-existing-dates guard, chronological/undated sorting, entity/session/tag linking with cross-campaign rejection, entity-filtered listing, `gm_only` visibility gating, and search integration respecting that visibility. Full existing suite (168 integration tests across 15 files) re-run clean, confirming no regression from the new calendar-validation guards on `campaigns`/`sessions`.
- Manually verified end to end against a freshly restarted dev stack (Playwright-driven, per this repo's convention): configured a custom calendar (added a 40-day "Frostwane" month), created a day-precision timeline event dated in it, confirmed the event page and list render `Frostwane 40, 100` using the custom month name (not a hardcoded Gregorian label), created an undated event and confirmed it appears under a separate "Undated" heading, and confirmed the World page's new "Timeline" link navigates correctly. Caught and fixed two real bugs in the process: `CalendarMonthsEditor`'s per-row `TextField`s had no `id`, so repeated "Name"/"Days" labels weren't uniquely associated with their inputs (broke both accessibility and this verification's own selectors); and a stale Vite `optimizeDeps` cache from an earlier session was serving an outdated `@worldbinder/validation` bundle, causing `DEFAULT_CALENDAR_CONFIG`-derived state to evaluate as `undefined` and crash the Settings page — fixed by clearing the cache and restarting the dev server, not a code change.
- **Scope notes**: no leap-year/era-cycle rules in v1 — calendars are a flat ordered month list with fixed day counts, no repeating leap adjustment (documented follow-up). No optimistic concurrency on timeline events, matching Maps' precedent (not a listed exit criterion). Filters are server-side query params, consistent with entities' list-query precedent, unlike Maps' client-side-only layer filters. World's Timeline entry point is a plain link on the World page header, not the roadmap's aspirational `World › Encyclopedia/Timeline/Relationships` tab-nav — that IA redesign is out of scope here since a Relationships tab doesn't exist yet either.

## [0.11.0] - 2026-07-13

### Added

- **Milestone 10 — Maps.** Map CRUD, map image upload (reusing Milestone 9's attachment pipeline), layers, pin placement/drag/delete, location links to any entity type, visibility controls, client-side layer/type filters, and an accessible pin list — both API and web UI.
- New schema: `maps` (`image_attachment_id` a direct nullable FK to `attachments`, `on delete set null` — same shape as `campaigns.cover_attachment_id`, not a `resource_attachments` join, since a map has at most one background image), `map_layers` (`display_order`, per-layer `visibility`), and `map_pins` (`x_normalized`/`y_normalized` as `double precision`, both constrained to `[0, 1]` at the Zod boundary so pins stay positioned across responsive image sizes). **`maps` hard-deletes** (no `deleted_at` column, unlike entities/sessions/plot_threads — matches the roadmap's literal §9.12 column list) and cascades to its layers and pins via FK; deleting a layer only `set null`s its pins' `layer_id`, ungrouping rather than deleting them. `map_pins.location_entity_id` accepts **any** entity type, not just `entityType === 'location'` — the column name describes "the location on the map," not a type constraint, and ui-ux.md explicitly lists Characters/Factions/Events/Quests as valid pin subjects.
- **Four-way visibility composition prevents a specific leak**: a pin is visible to a viewer only if the map, its own layer (if any), the pin itself, _and_ its linked entity (if any) all pass that viewer's visibility check — `MapsService.isPinVisible()`, a pure exported function, mirrors Milestone 7's `SearchService.searchRelationships` "no anchor entity, so every side must be checked explicitly" reasoning. Without this, a `public` pin on a `public` map with no layer could otherwise leak a `gm_only` entity's existence and map location to a player. A pin whose linked entity has been soft-deleted is hidden from everyone, including the GM. `MapsService.getById()`/`list()` 404 (never 403) a `gm_only` map to a non-privileged viewer, per ADR-0008.
- `maps` module (`apps/api/src/maps/`): one `MapsService` owns `maps`/`mapLayers`/`mapPins` (same "domain plus its join-ish tables in one service" shape as `PlotThreadsService`), gated by a new `CampaignPolicyService.canManageMaps` (owner/gm/editor, same actor set as `canManageAttachments`/`canManagePlotThreads`). The map-image `PATCH` validates a `ready`, same-campaign, image-typed attachment via a direct `attachments` table read — no `AttachmentsModule` import, same precedent as `CampaignsService`'s cover-image field. A narrow `PATCH .../pins/:pinId/position` endpoint backs pin dragging specifically — it accepts only the two coordinate fields, so a fast-firing drag update can never clobber a concurrent label/visibility edit from the full pin-edit form. No optimistic-concurrency check on maps/layers/pins (not a listed exit criterion for this milestone, and pins are frequently-repositioned data better served by last-write-wins). Deleting a map records a `campaign_audit_events` `destructive_action` row, same precedent as attachment deletion.
- Frontend `features/maps/`: `MapListPage`/`MapFormPage` (name/description/visibility plus the same upload-then-poll-until-ready-then-PATCH image flow `CampaignSettingsPage`'s cover image already uses) and `MapDetailPage`, which composes `MapCanvas` (the map image in a container whose CSS `aspect-ratio` is set from the attachment's stored width/height, so normalized-coordinate pins stay pixel-aligned to the same map feature at any viewport width with no resize-observer logic), `MapLayerToggles` (client-side layer/unlayered filters driving both the canvas and the list from one shared state), `AccessiblePinList` (a plain keyboard-reachable button list doing the same activation as clicking a visual pin — satisfies the "keyboard users can access all pin content" exit criterion without a parallel accessibility tree over the absolutely-positioned canvas), and `MapPinForm` (inline, no modal primitive exists in this codebase yet — same precedent as `RevisionHistoryPanel`). A GM/editor-only "manage mode" toggle governs the interaction model: view mode clicking a linked pin navigates to its entity page (ui-ux.md: "not a giant popup"; a freestanding label-only pin is inert), manage mode clicking opens the inline edit form and dragging (pointer events + `setPointerCapture`, with a small movement threshold to distinguish a click from a drag) repositions. "Maps" added to `CampaignLayout`'s nav between Threads and Search, matching the roadmap's literal §9 nav ordering.
- 6 new unit tests (`isPinVisible`'s truth table, including the public-pin/gm_only-entity leak case) and 1 new policy spec case (`canManageMaps`). 16 new integration tests (`maps.e2e-spec.ts`) covering map/layer/pin CRUD, cross-campaign rejection on all three, delete-cascade behavior (map delete removes layers/pins and audits; layer delete ungroups rather than deletes pins), the reposition endpoint's narrow field scope and range rejection, the full visibility-leak matrix (`gm_only` map, `gm_only` layer under a public map, a public pin linked to a `gm_only` entity, and a soft-deleted linked entity), and the map-image attachment gate's not-ready/cross-campaign/non-image rejections.
- Manually verified end to end against the running dev stack (Playwright-driven): created a map, uploaded an image and watched it move through pending → processing → ready via polling with correct aspect-ratio rendering; placed both an entity-linked pin and a freestanding labeled pin by clicking the canvas; dragged a pin to a new position; confirmed the accessible pin list mirrors the canvas exactly, including after toggling the "Cities" layer off (both surfaces hid the linked pin simultaneously); confirmed clicking a linked pin in view mode navigated to its entity page; set the map to `gm_only`, invited and accepted a real player-role member through the full registration/verification/invitation flow, and confirmed the map is absent from that player's map list and returns "This map could not be loaded" (a 404, not a 403) on direct navigation to its URL.
- **Scope notes**: no optimistic concurrency on maps/layers/pins (see above). Filters are client-side only — no new server query parameters, same reasoning as `PlotThreadPicker`. ui-ux.md's "pins can link to plot threads/session references/attachments" is narrower in this milestone than the aspirational UX sketch — the roadmap's literal §9.12 schema gives `map_pins` a single `location_entity_id` FK, so v1 pins link to at most one entity; no separate pin-to-plot-thread or pin-to-attachment link exists yet.

## [0.10.0] - 2026-07-12

### Added

- **Milestone 9 — Attachments.** Presigned uploads to S3-compatible storage (MinIO locally), a background worker that authoritatively verifies file type via hand-rolled magic-byte detection, computes a sha256 hash and image dimensions, a resource-attachment picker on entity/session/plot-thread detail pages, campaign cover images, short-lived signed downloads, and an hourly cleanup sweep for abandoned uploads — both API, worker, and web UI. This is the first milestone to give `apps/worker` real job-processing work (it was an idle scaffold since Milestone 0) and the first to add BullMQ or an S3 client anywhere in the monorepo.
- New schema: `attachments` (`storageKey` fully opaque — `attachments/{campaignId}/{attachmentId}`, no user-controlled filename/extension in the key at all; `uploadedByUserId` nullable with `onDelete: 'set null'`, deliberately not copying `entities.createdByUserId`'s cascade pattern — a cascaded user deletion would delete the row at the FK level with no application code running, so the cleanup job would never fire and the underlying storage object would become permanently unreachable garbage) and `resourceAttachments` (a real many-to-many join — one attachment can legitimately be linked to more than one resource — with `resourceType` as plain `text`, not an enum: `entityWikiLinks.sourceResourceType` already established this precedent for polymorphic discriminators expected to grow, and reusing Milestone 8's `resourceRevisionTypeEnum` here would have created a real DB-level coupling between two unrelated features). Same migration adds the FK `campaigns.coverAttachmentId → attachments.id` that Milestone 2 stubbed as an unconstrained column.
- New `apps/api/src/storage/` (`StorageService`: presigned PUT/GET via `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`, `HeadObjectCommand`-verified completion, best-effort `DeleteObjectCommand`, `onModuleInit` bucket auto-creation outside production) and `apps/api/src/jobs/` (`AttachmentQueueService`, a BullMQ producer with its own Redis connection — `maxRetriesPerRequest: null`, distinct from the `@Global()` rate-limiting `REDIS` token — placed as a sibling of `attachments/`, not inside it, per the roadmap's own directory sketch, anticipating Milestone 12's export/import queues will reuse it). `apps/worker` gained matching `storage/`/`jobs/` modules plus `attachments/process-attachment.ts` and `attachments/cleanup-sweep.ts` — both plain exported async functions (dependency-injected `pg.Pool` + `S3Client`, not framework DI), callable both by the real BullMQ `Worker` and directly by integration tests, so `pnpm test:integration` never needs a live queue consumer running.
- **Hand-rolled magic-byte sniffing instead of the `file-type` npm package**: `apps/api` and `apps/worker` both compile to CommonJS (no `"type": "module"`), and `file-type` has been ESM-only since v17. PNG/JPEG/GIF/WebP/PDF each have trivial fixed signatures, so `packages/validation/src/attachment-detection.ts` implements `sniffMimeType()` directly — sidesteps the dependency entirely and is trivially unit-testable with fixture buffers. Plain text and Markdown have no magic number by design, in real tension with "do not trust extension" (roadmap §16.2) — mitigated by a `looksLikeText()` heuristic (null-byte and control-byte-ratio checks on the first 8KB); both collapse to a single detected `text/plain` value server-side, since Markdown-vs-plain-text cannot be verified any other way. `image-size` (a real dependency, not hand-rolled — per-format dimension parsing is intricate enough to justify one) extracts width/height for image types.
- **The worker's processing is asynchronous, and the API surface is built around that, not around pretending it isn't**: `complete()` verifies the object actually landed in storage (`HeadObjectCommand`) and enqueues processing, but does not wait for it — `link()` and the cover-image `PATCH` both require `status: 'ready'` and reject with `409`/`403` otherwise. A new `GET /campaigns/:campaignId/attachments/:attachmentId` status-check endpoint backs the frontend's poll-until-ready step between upload and link — this exact race (the frontend originally called `link()` immediately after `complete()`, before the worker had finished) was caught via live browser testing, not by any automated test, and fixed by adding the poll rather than by weakening `link()`'s `ready`-only guard.
- **Download authorization is resource-scoped by construction, not by a fallback rule.** `GET /campaigns/:campaignId/attachments/:resourceType/:resourceId` lists attachments linked to _that_ resource, gated purely by that resource's current live visibility — no `@RequireCampaignRole` guard, the same shape as Milestone 8's `RevisionsController.list()`. A viewer who can see resource A sees A's attachment list regardless of whether the same attachment is also linked to a `gm_only` resource B, because they never issue a request in B's context — the "does visibility need to check any-vs-all linked resources" question this design could have run into never has to be answered in code. Each entry carries a freshly generated, short-lived (~15min) signed download URL embedded directly in the response.
- Campaign cover images extend the _existing_ `PATCH /campaigns/:campaignId` (`coverAttachmentId`, validated via a direct `attachments` table read inside `CampaignsService` — matching its dashboard method's existing "read a sibling table directly" precedent — rather than importing `AttachmentsService`) instead of a new dedicated endpoint. Exposed to every active member regardless of role, the same way a campaign's name/description already has no visibility gate.
- `CampaignPolicyService.canManageAttachments` (owner/gm/editor, same actor set as `canEditEntities`/`canManagePlotThreads`, kept as its own method per the existing "distinct capability that could diverge later" convention).
- New `packages/ui/src/FileDropzone.tsx` (click-to-browse + drag-and-drop; no file-input primitive existed in this package before). New `apps/web/src/features/attachments/`: an `AttachmentsPanel` mirroring `RevisionHistoryPanel`'s self-contained shape exactly, wired into the entity/session/plot-thread detail pages between "Plot Threads" and "Backlinks"/"Revision History" per `docs/planning/ui-ux.md`'s layout sketch; a dedicated `uploadToStorage()` helper that deliberately bypasses `apiClient.ts` (no `Authorization`/`credentials` sent to the storage origin); the first `refetchInterval` polling precedent in this codebase, keeping the panel live while the worker processes an upload. A small cover-image control on `CampaignSettingsPage`.
- 21 new worker unit tests (`sniffMimeType`/`looksLikeText` fixture buffers covering every allowed type plus rejection cases; `processAttachment()` and `cleanupAbandonedAttachments()` with injected fake `pg.Pool`/`S3Client`, no real infrastructure needed) and 1 new policy spec case (`canManageAttachments`). 18 new integration tests (`attachments.e2e-spec.ts`) running the real presign → PUT-to-MinIO → complete pipeline end to end (not mocked), plus permission gates, the not-ready/cross-campaign link rejections, resource-scoped visibility (a `gm_only`-linked attachment invisible to a player), the unlinked-attachments management list, delete + its `destructive_action` audit event, and all three cover-image validation paths. The worker's own file-processing correctness (magic bytes, hashing, dimensions) is covered by its unit tests with fakes rather than duplicated in the API's integration suite, which instead verifies the API surface built on top of the resulting `ready` state.
- Manually verified in a real browser (Playwright-driven): uploaded an image to an entity's Attachments panel and watched it move through the pending → processing → ready states via polling, confirmed the thumbnail and signed download URL render correctly; uploaded a campaign cover image through the same upload → poll → PATCH flow and confirmed it's visible to a player-role member. Also manually verified end to end via curl against a live worker process: the full pipeline (including a genuinely corrupt "PNG" that passes the magic-byte check but fails `image-size` parsing, correctly rejected) and the cleanup sweep (a backdated `pending` row correctly rejected and its storage object removed).
- **Scope notes**: `attachments.visibility` is schema-complete (per the roadmap's literal column list) but not enforced by any v1 read path — no UI surface reaches an attachment with zero `resource_attachments` links other than the cover-image flow, which deliberately doesn't gate on it either (campaigns have no visibility concept). 20MB size ceiling and the 24h-abandonment/hourly-cadence cleanup sweep are documented judgment calls, same spirit as Milestone 8's `REVISION_WINDOW_MINUTES`. Animated-GIF detection is out of scope (roadmap says "optionally static-only"). No automated integration test exercises the cleanup sweep against real Postgres (it's covered by unit tests with fakes plus a manual live run) — adding a live-worker-process integration test was judged not worth the added test-environment complexity for this milestone.

## [0.9.0] - 2026-07-12

### Added

- **Milestone 8 — Revisions and Audit.** Immutable revision snapshots for entities, sessions, and plot threads with autosave-window grouping, a revision-history panel with field-level diff and one-click restore, and a new campaign-scoped audit/activity log covering role changes, member removal, content reveal, revision restore, and destructive actions — both API and web UI.
- New schema: `resource_revisions` (`resourceType` enum of `entity`/`session`/`plot_thread`, full GM-inclusive `snapshot_json`, `revision_number`, nullable `change_summary`, `created_by_user_id`) and `campaign_audit_events` (`type` enum, nullable `actor_user_id`/`target_resource_type`/`target_resource_id`, structured `metadata_json` only — never content bodies, per roadmap §11.14). No update/delete method exists anywhere on `CampaignAuditService`, and no controller mutation route was ever added — that omission is the entire mechanism behind the "audit records cannot be edited through normal APIs" exit criterion, not a permission check that could be bypassed.
- **Two dependency-free leaf modules avoid a circular-dependency dead end.** Recording a revision must happen _inside_ `EntitiesService`/`SessionsService`/`PlotThreadsService`'s `create()`/`update()`; restoring one must replay the _full_ owning-service write path (not a raw `UPDATE` that would skip tag sync, wiki-link refresh, join-table resync, or tsvector rebuild) — the opposite dependency direction. `RevisionRecorderModule`/`RevisionRecorderService` (only `recordRevision()`, only `@Inject(DRIZZLE)`) and `CampaignAuditModule`/`CampaignAuditService` (`record()`/`list()` only) are the leaves that `EntitiesModule`/`SessionsModule`/`PlotThreadsModule`/`MembershipModule`/`CampaignsModule` import one-directionally to write; `RevisionsModule` and `CampaignAuditViewModule` are separate orchestrator modules that import the owning modules the other way, to list/restore. No `forwardRef()` anywhere (still unused in this codebase). Restore-mapping logic (`entitySnapshotToUpdateInput`/`sessionSnapshotToUpdateInput`/`plotThreadSnapshotToUpdateInput`) lives as pure functions exported next to each owning service rather than new DI surface.
- **Windowed merge grouping**: same actor + same resource + within a 30-minute window (`REVISION_WINDOW_MINUTES`, a documented judgment call in the spirit of `plot-threads.service.ts`'s `NEGLECT_THRESHOLD_SESSIONS`) updates the latest revision row's snapshot in place rather than inserting a new one — autosave-driven edits during one sitting collapse to a single revision instead of one per keystroke-triggered save. A `recordRevision()` bug where a merged row's `changeSummary` stayed stuck on a stale value (e.g. "Restored from revision #1" surviving into a later plain edit) was caught via live manual testing, not the automated suite, and fixed by making the field unconditionally overwritten on every merge.
- **Restore always creates a new, distinct revision — never merges into the restoring actor's own recent edit window.** `recordRevision()` takes an `allowMerge` flag: `true` for normal `create()`/`update()` calls, `false` when the write originates from `RevisionsService.restore()`. Restore replays the snapshot through the real owning-service `update()` (full side effects, not a raw write), reuses the live resource's current `updatedAt` so the optimistic-concurrency check is a no-op in the normal case while still catching a genuine race, and rejects with `409` if the live resource has since been soft-deleted rather than producing a confusing generic `404`.
- **Field-level diff without a diffing library** (none — `diff`/`jsdiff`/`fast-diff` — is installed in this repo, and adding one for a single read endpoint wasn't judged worth it). `RevisionsService.list()` computes plain-text extracts per content field server-side (reusing Milestone 7's `extractPlainText`) and ships them alongside the raw snapshot, so the frontend does a trivial string comparison instead of walking TipTap JSON. Scoped to a changed-field list with before/after plain text, not a word-level diff — noted as a follow-up rather than a dependency added for v1.
- **GM-content omission is enforced at read time, not write time.** Snapshots always store the full GM-inclusive shape; `RevisionsService.list()`/`restore()` redact or omit GM-only fields based on the _viewing/restoring_ actor's _current_ permission, not the snapshot's original author's permission — matching roadmap §13.1's "revision history must follow the current resource permission policy" without needing two stored variants per snapshot.
- `GET /campaigns/:campaignId/revisions/:resourceType/:resourceId` and `POST /campaigns/:campaignId/revisions/:revisionId/restore` (both in the roadmap's own route table); `GET /campaigns/:campaignId/audit` (owner/gm-gated, the first _read_ route in this codebase to use `@RequireCampaignRole` — not in the roadmap's literal route table, which is representative rather than exhaustive, but structurally necessary to back the required audit activity view).
- Frontend: one shared `RevisionHistoryPanel` (`features/revisions/`) reused identically on the entity/session/plot-thread detail pages — same self-contained, own-data-fetching shape as Milestone 4's `RelatedContentPanel`. Restore uses `window.confirm(...)`, matching every other confirmation in this codebase (no modal component exists in `packages/ui` yet, and this wasn't the milestone to add the first one). A new `AuditPage` (`features/audit/`) at `/app/campaign/:id/audit`, linked from a new "Activity log" section on the owner/gm-gated Settings page rather than the roadmap's fixed primary nav (which has no slot for it).
- 7 new unit tests (`shouldMergeRevision`'s merge-vs-insert boundary, factored out of `RevisionRecorderService` the same way `isNeglected`/`checkRelationshipTypeCompatibility` were) and 15 new integration tests (`revisions.e2e-spec.ts`, `audit.e2e-spec.ts`) covering merge-window grouping, restore always producing a distinct revision even immediately after the restoring actor's own edit, cross-campaign spoofing rejection on both list and restore, soft-deleted-resource restore rejection while history stays listable, GM-content omission on both revision snapshots and hidden-resource history, and audit events recorded for role change, member removal, content reveal, revision restore, campaign archive, and destructive deletion.
- Manually verified in a real browser (Playwright-driven, per this repo's UI-verification convention): a GM and a second "editor" account produced two distinct, differently-authored revisions on an entity; the revision-history panel listed both with correct actor/timestamp, "View changes" expanded a field-level diff (summary and tags), and restoring an older revision reverted the live content and appended a new, unmerged revision rather than silently folding into anything. The Settings page's new "Activity log" link correctly reached `/audit`, which rendered the recorded `revision_restored` event.
- **Scope notes**: relationships are excluded from revisions (a small structured link record, not long-form content — matches how relationship edits are already treated as single-field changes elsewhere); no revision snapshot is taken on delete (soft-deleting a resource doesn't itself create a final revision); login/password/session security events stay on the existing auth-only `security_events`/`AuditService` and aren't duplicated here; campaign export/import audit events are deferred (Milestones 12/13 don't exist yet).

## [0.8.0] - 2026-07-12

### Added

- **Milestone 7 — Search.** Full-text search across entities, sessions, plot threads, and relationship descriptions, with weighted ranking, `pg_trgm` fuzzy/prefix name matching, a global `Ctrl`/`Cmd+K` search overlay, a dedicated results page with type filters and pagination, keyboard navigation, and highlighted snippets — both API and web UI.
- New schema: `search_vector_public`/`search_vector_gm` `tsvector` columns on `entities`, `sessions`, and `plot_threads` (via a shared `customType` helper — no native tsvector column type in drizzle-orm), plus a single `search_vector` column on `entity_relationships` (weight D, description only — no public/GM split exists on that table). **Two vector columns per table, not one gated at query time**: `searchVectorGm` additionally folds in secret content (`gmContentJson`/`plannedContentJson`), so a non-GM viewer's query can never match against hidden text — the same field-omission stance this codebase already takes on `gmContentJson` itself, applied one layer earlier so it can't leak through result _presence_, not just result _content_. Row-level `visibility` stays a separate, still-mandatory filter on top of this — the vector split only gates content within an otherwise-visible row. New GIN indexes on every tsvector column, `gin_trgm_ops` trigram indexes on each table's name/title column, and a leading `campaign_id` index on all four tables (none existed before — Postgres doesn't auto-index FK columns, and none of the four search-touched tables had ever needed one until now).
- **Documented migration deviation**: enabling `pg_trgm` requires one `CREATE EXTENSION IF NOT EXISTS pg_trgm;` statement that `drizzle-kit generate` cannot produce from `schema.ts`, and `infrastructure/migrations/` is hook-blocked from direct edits by design. Per the `db-migration` skill's own guidance for cases drizzle can't express, this one line was added by hand to `infrastructure/migrations/0007_flawless_impossible_man.sql` outside the normal generate-only flow — everything else in that migration is machine-generated.
- `search` module (`apps/api/src/search/`): `search-vector.util.ts` (`extractPlainText` — a new TipTap-JSON-to-plain-text walker, modeled on `WikiLinksService.extractMentions`'s recursion shape; `buildWeightedTsvector` — builds the `setweight(to_tsvector(...), 'A'..'D') || ...` SQL expression used at every write site). `SearchService` follows the `campaigns.service.ts` dashboard's precedent exactly: injects only `DRIZZLE` + `CampaignPolicyService` (no sibling services), runs four independent per-table queries rather than a UNION (nothing in this codebase uses UNION), and merges/re-ranks the results in JS. Each query computes a ranking tier (roadmap §14.3: exact name → exact alias → name prefix → fuzzy name → tag/summary → content → relationship description) and a score via `ts_rank_cd`/`similarity()`/`ilike`, filtered by the same conditional-visibility-array pattern every other list query already uses. The relationship query joins `entities` aliased twice (source/target) and requires **all three** of the relationship's own visibility and both endpoints' visibility to pass — stricter than `RelationshipsService.canSeeRow`'s one-sided check, which is safe there only because it's always called from an already-visible anchor entity; global search has no anchor, so both sides must be checked explicitly in SQL (roadmap §14.5: filter inside the query, never rank-then-drop).
- Snippets are built from plain-text substring matching with recorded highlight offsets (`{ text, highlights: [number, number][] }`), not `ts_headline()` — this codebase doesn't persist raw plain-text bodies anywhere (only the derived tsvector), and running a JS substring scan over the handful of already-fetched, already-`LIMIT`-ed result rows is cheaper than adding storage just to feed `ts_headline`. Offsets only, never embedded markup, so the frontend renders via plain text nodes wrapped in `<mark>` — no `dangerouslySetInnerHTML` anywhere in this feature, consistent with the rest of the app.
- `GET /campaigns/:campaignId/search` (new `SearchController`, guarded the same as every other campaign-scoped route) serves both the overlay (small `limit`) and the results page (paginated, `types`-filtered) from one endpoint, matching the roadmap's single documented route. Plain `limit`/`offset`, not `packages/validation`'s existing `cursorPaginationQuerySchema` — that schema has zero call sites anywhere in the repo and doesn't fit a JS-merged, re-ranked result set from four independent queries.
- Frontend `features/search/`: `SearchOverlay` (portal-rendered modal, 200ms-debounced query matching `EntityPicker`'s existing debounce pattern, arrow-key nav modeled on `packages/ui/src/Combobox.tsx`'s modulo-wrap handling — referenced as a pattern, not reused, since `Combobox` is a single-input-bound field, not a multi-type modal), `SearchResultsPage` (URL-driven `q`/`types`/`page`, matching the roadmap's "search terms live in the URL" state-ownership rule), and a new `useSearchOverlayStore` Zustand store holding overlay open/query state only. `CampaignLayout` gained a "Search" nav link and the `Ctrl`/`Cmd+K` keydown listener. Relationship results have no standalone page to link to — they navigate to the source entity's detail page instead (same scope shape as Milestone 4's "relationships surface as panels, not their own pages" decision).
- **Scope note**: Timeline events (roadmap §14.1) are not part of this milestone's search coverage — no timeline/calendar module exists yet (Milestone 11), so there's nothing to index. Same honest-scope-note spirit as Milestone 4's "no `/world/relationships` graph view this milestone."
- 9 new unit tests (`extractPlainText`, `buildWeightedTsvector`, and `searchQuerySchema` — the explicit §20.1 "Search query parsing" unit-test target) and 9 new integration tests (`search.e2e-spec.ts`) covering ranking-tier ordering, cross-campaign isolation, hidden-entity exclusion, hidden-relationship exclusion via the two-sided endpoint check, GM-only content never matching a player's query even when the parent entity is public, and pagination/type-filtering.
- New `apps/web/e2e/search.spec.ts`: a GM finds both a public and a GM-only entity (and a phrase buried in GM-only content) via the overlay and the results page; an invited player finds the public entity by name but never the GM-only entity or the secret phrase, through either surface — the roadmap §20.4 step 14 ("Search visible content") E2E target.
- **Performance validation**: new `apps/api/src/database/seed-perf.ts` (`pnpm db:seed:perf`) batch-inserts a 10k-entity/50k-relationship/200-session/2k-plot-thread campaign directly via Drizzle — bypassing the service layer for speed, but building tsvectors through the same `search-vector.util.ts` functions the real services use, so the fixture matches production writes rather than a special-cased backfill. New `apps/api/src/search/search-benchmark.ts` (`pnpm search:benchmark`) runs representative queries (exact/prefix/fuzzy name, content word, relationship-description word, multi-word phrase) against the seeded campaign and prints p50/p95/p99 — a plain script, not a Jest test, since a hard latency assertion against a shared local Postgres would be flaky in CI. Measured **p95 = 337ms** against the roadmap §14.4 budget of 500ms — exit criterion met. This is a scoped-down harness for this milestone's own criterion only; the full §20.6 cross-feature profiling suite (dashboard, entity details, relationship queries, exports, imports) is deferred to Milestone 14.

## [0.7.0] - 2026-07-12

### Added

- **Milestone 6 — Plot Threads and Dashboard.** Plot-thread CRUD with status/importance, related entities, session-linking with an `introduced`/`advanced`/`resolved` action (recorded from the session's side, matching the roadmap's own E2E ordering), server-computed dormancy ("neglected") flagging, the §9.8 GM→player status projection, and a real campaign Dashboard aggregating current world date, upcoming/last session, active and neglected threads, and a recent-activity feed — both API and web UI.
- New schema: `plot_threads` (mirrors entities' public/GM-content split + soft delete + optimistic concurrency), `plot_thread_entities` (composite-key join, no surrogate id — same minimal shape as `entity_tags`/`session_entities`), and `session_plot_threads` (deferred from Milestone 5 pending this table's existence — composite key plus an `action` enum, since ui-ux.md's session-detail sketch shows "Threads Advanced" and "Threads Created" as distinct groupings, not a plain link).
- `plot-threads` module (`apps/api/src/plot-threads/`): three pure, unit-tested functions carry the interesting logic — `projectPlayerFacingStatus` (the GM-facing enum is never sent to a non-GM viewer, same field-omission rule as `EntityDetail.gmContentJson`), `computeResolvedSessionTransition` ("plot-thread transitions," an explicit roadmap §20.1 unit-test target: entering `resolved` records which session resolved it, leaving `resolved` clears that record), and `isNeglected` ("dormancy calculations" — unresolved and not referenced by a session in the last `NEGLECT_THRESHOLD_SESSIONS` (3, a documented judgment call, not roadmap-specified) played sessions). `CampaignPolicyService` gained `canManagePlotThreads` (owner/gm/editor, same actor set as `canEditEntities`/`canEditSessions`).
- `SessionsService` now depends on `PlotThreadsModule` (one-directional — `PlotThreadsModule` doesn't depend back on `SessionsModule`, reading the `sessions` table directly instead, same shape as the M4 `RelationshipsModule`/`EntitiesModule` relationship) to full-replace-sync `session_plot_threads` and apply `PlotThreadsService.applySessionLink`'s side effects only for links that are new or changed action — not re-applied on every unrelated session edit, and deliberately **not reversed if a link is later removed** (documented simplification, same accepted-drift spirit as Milestone 5's session-number generation).
- `GET /campaigns/:campaignId/dashboard` on the existing `CampaignsController`/`CampaignsService` (roadmap §11.2 assigns "Dashboard aggregation" to the Campaign module, not a new one) — reads `entities`/`sessions`/`plot_threads` directly rather than injecting their services, the same precedent `SessionsService` already set writing `campaigns.currentWorldDateJson` directly. **Scope note:** ui-ux.md's dashboard sketch shows "Recently Edited" and "Recent Activity" as two separate widgets; there's no activity-log table in the data model (only `security_events`, which is auth-only), so one honest "recently changed" feed backs both rather than fabricating a second data source.
- **Routing decision:** the roadmap's nav model is `Dashboard, World, Sessions, Threads, Maps, Search` — no "Overview" item. Rather than add a redundant `/dashboard` route alongside the Milestone 2-era "Overview" page, this milestone upgrades that same index route in place (renamed nav label, real dashboard widgets added to the existing `CampaignOverviewPage` component) instead of the roadmap sketch's literal `/dashboard` path.
- Frontend `Threads` section: `/app/campaign/:id/threads` (list, with "Unresolved" and "Neglected" panels), `/threads/new` and `/threads/:threadId/edit` (form), `/threads/:threadId` (detail — gated status/importance, related entities, session timeline). New `PlotThreadPicker` (client-side-filtered over the campaign's full thread list rather than a server search endpoint — campaigns have far fewer plot threads than entities) and `PlotThreadChangesEditor` (the session-side linking UI), both under `features/plot-threads/`. `SessionFormPage`/`SessionDetailPage` extended with plot-thread-change editing and "Threads Created/Advanced/Resolved" groupings.
- 10 new unit tests (`projectPlayerFacingStatus`, `computeResolvedSessionTransition`, `isNeglected`, `canManagePlotThreads`) and 21 new integration tests (`plot-threads.e2e-spec.ts`, plus session-linking and dashboard cases added to `sessions.e2e-spec.ts`/`campaigns.e2e-spec.ts`) covering CRUD, related-entity sync, visibility/field-omission, the full GM→player status projection table, session-link side effects (introduced-sets-once, resolved-sets-status, higher-session-number-supersedes, unlink-doesn't-retroactively-clear), cross-campaign rejection, and dashboard aggregation (neglect-after-gap, upcoming/last-session selection, player field omission).
- New `apps/web/e2e/plot-threads.spec.ts`: full lifecycle — introduce a thread in a session, let three more sessions pass, confirm it surfaces under "Neglected" on both the Threads page and the Dashboard, resolve it via a session link, and confirm a player sees the projected "Completed" label while the GM still sees the internal "resolved" status.

### Fixed

- `apps/web/e2e/relationships.spec.ts`'s backlink assertion used `getByText('Backlinks')`, which is a case-insensitive substring match — it intermittently collided with the empty-state text "No backlinks yet." under full-suite load (passed in isolation, flaked when run alongside the rest of the suite). Found while chasing the identical bug pattern in the new plot-threads spec's own assertions. Tightened to `getByRole('heading', { name: 'Backlinks' })`.

## [0.6.0] - 2026-07-12

### Added

- **Milestone 5 — Sessions.** Session list/detail/editor with planned/in-progress/completed/cancelled states, participants (real campaign members, distinct from in-fiction featured entities), featured entities, locations visited, public recap vs. GM-only/planned-content split, an atomic session-completion transaction, and a secret-reveal workflow — both API and web UI.
- New schema: `sessions`, plus join tables `session_participants`, `session_entities`, `session_locations` (composite-key, no surrogate id — same minimal shape as `entity_tags`), and `session_reveals` (an audit trail, not just a link — "this entity's visibility flipped from `gm_only` to `public` during this session"). `sessions` mirrors entities' public/GM-content split, soft deletion, and optimistic-concurrency (`updatedAt`-based `409`) shape exactly. `session_number` is server-assigned per campaign (next available, same accepted-low-risk approach as entity slug generation — no serializable-retry machinery).
- A minimal versioned in-world date shape, `WorldDate` (`{schemaVersion, year, month, day, label?}`), used by `sessions.world_start_date_json`/`world_end_date_json` and (newly typed, previously an untyped JSON blob) `campaigns.current_world_date_json`. Deliberately Gregorian-shaped and not yet validated against a per-campaign calendar — Milestone 11 ("Timeline and Calendar") owns real calendar settings.
- `sessions` module (`apps/api/src/sessions/`): CRUD with full-replace sync of participants/featured-entities/locations (same pattern as entity tag sync, cross-campaign IDs rejected, locations validated as `entityType === 'location'`); `POST .../complete` atomically flips status, finalizes recap/world-end-date, and — if a world-end-date is resolved — advances `campaigns.current_world_date_json`, all in one transaction, with a DB-level guard (409 on an already-completed session) standing in for the idempotency-key mechanism roadmap §18.4 mentions but that no infrastructure in this codebase implements yet; `POST .../reveals` is a deliberately standalone action (usable before, during, or after completion) that flips a `gm_only` entity to `public` and is self-idempotent (400 on an already-public entity). `CampaignPolicyService` gained `canEditSessions` (owner/gm/editor, same actor set as `canEditEntities`) and `canRevealContent` (owner/gm only, per §5.6's "Reveal content" row).
- `GET /campaigns/:campaignId/entities/:entityId/sessions` (new route on `EntitiesController`, same cross-module-service pattern as Milestone 4's relationships/backlinks routes) backs the new "Session Appearances" panel on the entity detail page — the union of `session_entities` and `session_locations` links for that entity, visibility-filtered.
- Frontend `Sessions` section: `/app/campaign/:id/sessions` (list), `/sessions/new` and `/sessions/:sessionId/edit` (form — participants checklist, `EntityMultiPicker` for featured entities/locations), `/sessions/:sessionId` (detail — recap/gated planned+GM sections, a "Complete session" mini-form, a "Discoveries" reveal panel). New `EntityMultiPicker` (`features/entities/components/`, composing Milestone 4's `EntityPicker` with a chip list) — the multi-select counterpart used wherever a session links several entities at once.
- 2 new unit tests (`canEditSessions`/`canRevealContent`) and 14 new integration tests (`sessions.e2e-spec.ts`) covering sequential session numbering, join-table full-replace sync and cross-campaign rejection, `gm_only`-session visibility (404 for players), planned/GM content omission, atomic completion (including the campaign world-date advance, verified via a second request), the already-completed 409, reveal's visibility flip and idempotency guard, reveal's owner/GM-only permission, and entity session-appearances via both featured-entity and location links.
- Extended `apps/web/e2e/sessions.spec.ts`: full lifecycle — create a session with a featured entity, complete it with an in-world end date, reveal a `gm_only` entity and confirm a player can see it afterward, confirm a player never sees planned/GM-only session content, confirm the featured entity's own page lists the session under "Session Appearances."

## [0.5.0] - 2026-07-12

### Added

- **Milestone 4 — Relationships, Wiki Links, and Backlinks.** Typed relationships between entities (14 built-in types plus per-campaign custom types), `[[wiki-link]]` mentions inside rich text, backlink tracking, and the incoming/outgoing/backlinks panels that surface them on the entity detail page — both API and web UI.
- New schema: `relationship_types` (built-ins have `campaign_id = null`, shared across every campaign; custom types are per-campaign), `entity_relationships` (no reverse row is ever stored — the reverse direction is a query projection using the type's `reverseLabel`), `entity_wiki_links`, plus a `wiki_link_section` enum. `relationship_types.key` uniqueness needed two _partial_ unique indexes rather than one composite constraint, since Postgres treats every `NULL` as distinct in an ordinary unique index and would otherwise let two built-in rows share a key.
- `relationships` module (`apps/api/src/relationships/`): relationship-type list/create, relationship create/update/delete, and a "neighborhood" query (`GET .../entities/:entityId/relationships`) that returns both directions already labeled and visibility-filtered. Built-ins are provisioned idempotently via `RelationshipTypesService.onModuleInit` (`onConflictDoNothing` targeting the primary key — the partial-unique-index-on-`key` approach doesn't work as an `ON CONFLICT` arbiter without repeating its predicate, and hand-editing a migration to `INSERT` seed rows isn't allowed, so each built-in type has a fixed id instead).
- `entities/wiki-links.service.ts`: extracts `entityMention` nodes from TipTap JSON on every entity save, validates every mention resolves to a live entity in the same campaign (a cross-campaign or dangling mention is a hard `400`, not a silent drop — same tenant-isolation stance as ADR-0008), and full-replaces the stored links per content section (same pattern as tag sync). Backlinks and relationships share one visibility predicate now: `CampaignPolicyService.canViewVisibility` (`EntitiesService.requireVisibleEntity` was refactored onto it too) — a public relationship or backlink pointing at a `gm_only` entity is invisible to anyone who can't see that entity, not just entities themselves.
- Frontend: a `[[`-triggered entity-mention autocomplete in `RichTextEditor` (new `@tiptap/suggestion`-based `entityMention` node, hand-rolled popup rather than pulling in `tippy.js`), a new `packages/ui` `Combobox` primitive plus an `EntityPicker` built on it (now also used to fill in the `*EntityId` cross-reference metadata fields — `currentLocationEntityId`, `leaderEntityId`, etc. — that Milestone 3 explicitly left unwired), and a `RelatedContentPanel` on the entity detail page combining relationships (with an inline "+ Relationship" create form) and backlinks.
- 4 new unit tests (`checkRelationshipTypeCompatibility`, factored out of `RelationshipsService` specifically so compatibility logic doesn't need a database to test) plus 5 (`WikiLinksService.extractMentions`) plus 2 (`CampaignPolicyService.canViewVisibility`), and 15 new integration tests (`relationships.e2e-spec.ts`, `wiki-links.e2e-spec.ts`) covering reverse-label projection, cross-campaign rejection, duplicate/symmetric-duplicate detection, type-compatibility rejection, backlink refresh-on-update, and the visibility-leak cases (hidden relationship, public relationship to a hidden entity, GM-only-section backlink). Extended `apps/web/e2e/relationships.spec.ts`: relationship creation + reverse projection, a `[[` mention creating a backlink, and a player confirmed unable to see a `gm_only` relationship.
- Scope note: no `/world/relationships` graph or list view this milestone — relationships surface as per-entity panels only, matching the milestone's actual deliverables rather than the later portfolio "relationship graph" polish item (roadmap §30).

### Fixed

- `@tiptap/suggestion`'s `allowSpaces` defaults to `false`, which silently closes the mention popup the instant the user types a space — fatal for a picker whose whole purpose is searching multi-word entity names ("Westvale Village"). Found via a real browser run (typing `[[Duke Ren` looked fine; `[[Duke Renald` didn't), not by the Jest/unit suites, which don't drive a real ProseMirror `contentEditable`. Fixed by setting `allowSpaces: true` on the extension's `Suggestion` config.

## [0.4.0] - 2026-07-11

### Added

- **Milestone 3 — Encyclopedia Core.** Campaign-scoped entities across all 11 core types (Character, Location, Faction, Organization, Item, Deity, Creature, Event, Quest, Lore, Custom), each with a type-specific JSONB metadata schema, tags, aliases, public/GM-only content split, visibility controls, soft deletion, and list filters (type/tag/name search) — both API and web UI.
- New schema: `entities`, `tags`, `entity_tags`, plus `entity_type`/`entity_status`/`entity_visibility` enums. Metadata schemas are versioned via an embedded `schemaVersion` key rather than a DB column (no existing precedent for the latter; see `packages/validation/src/entities.ts` for all 11 per-type schemas, combined into the repo's first `z.discriminatedUnion` on `entityType`).
- `entities` module (`apps/api/src/entities/`): CRUD, transactional tag find-or-create-and-link, campaign-scoped slugs, soft delete, list filters, and optimistic concurrency (`updatedAt`-based, `409 Conflict` on a stale write per roadmap §15.2). `CampaignPolicyService` gained `canEditEntities` (owner/GM/editor — Player is "Optional" per §5.6 but v1 has no toggle for it yet, so Player/Viewer stay read-only).
- **Field-level filtering (§13.2) has its first real implementation.** `EntityDetail.gmContentJson` is a genuinely optional contract field, included in the API response only when `canViewGmContent` allows it — omitted entirely otherwise, never sent as `null`. The same check also gates whether a `gm_only`-visibility entity is visible at all (a 404, not a 403, to a player — same non-disclosure reasoning as ADR-0008). See [ADR-0009](docs/decisions/0009-entity-visibility-two-tier.md) for why entity visibility is a plain two-tier enum rather than the roadmap's fuller per-member grants model.
- Frontend `World` section: `/app/campaign/:id/world` (list + filters), `/world/new` and `/world/:entityId/edit` (create/edit form), `/world/:entityId` (detail view) — all behind the existing `CampaignLayout`. New `packages/ui` primitives `Textarea` and `TagInput`.
- **TipTap rich-text editor** (`@tiptap/react` + StarterKit + Link + Table extensions) for public and GM-only content, with an accessible toolbar (roadmap §21). Image and entity-wiki-link/mention nodes are deliberately not included yet — both are genuinely blocked (no attachment upload pipeline until Milestone 9, no link-resolution backend until Milestone 4's wiki-link module), not just deferred by preference.
- **Autosave with IndexedDB draft recovery** (`idb`): edits to an existing entity debounce into a PATCH ~2s after idling; on failure (offline or a 409 conflict) the pending change is kept in IndexedDB instead of lost, with `Saving/Saved/Offline/Save failed` status shown near the editor and a restore-or-discard banner on next visit. A 409 conflict surfaces a small inline "changed elsewhere" notice (reload vs. keep-mine) rather than silently overwriting — no diff view yet, that's later UX polish.
- Extended the Milestone 2 Playwright scaffolding: `apps/web/e2e/entities.spec.ts` covers the full entity lifecycle (create with tags/metadata/split content, a player seeing public but not GM content, edit, tag filter, delete) plus a real offline-mid-edit probe using `context.setOffline()` to verify the draft survives a dropped connection and syncs once back online. Shared registration/login/campaign-setup helpers extracted to `apps/web/e2e/helpers.ts` (now a third consumer, `apps/web/playwright.config.ts` pinned to `workers: 1` to match the Jest e2e suite's `maxWorkers: 1` reasoning — specs share real IP-scoped rate limits and a real Mailpit inbox).
- 12 new unit tests (`canEditEntities` cases) and 11 new integration tests (`entities.e2e-spec.ts`) covering CRUD, tag reuse, cross-campaign isolation, soft delete, the stale-write 409, and the field-omission/entity-visibility checks.

### Fixed

- `apps/web/eslint.config.js` now ignores `playwright-report/` and `test-results/` — without it, `eslint .` was linting Playwright's own generated (minified) HTML-report JS bundle, producing thousands of spurious `no-undef`/`no-unused-expressions` errors the moment those directories existed locally.
- `packages/ui`'s `TagInput` didn't associate its `<label>` with its `<input>` (no `htmlFor`/`id`), so it was both an accessibility gap (§21: "proper form labels") and invisible to `getByLabel` in tests. Fixed via `useId()`.
- The Milestone 3 entity forms use plain `useState` rather than react-hook-form (the discriminated-union input types don't type-check cleanly against RHF's `register()` across a dynamic per-type field set), which meant none of their `TextField`/`Textarea`/`Select` fields had a `name`/`id` — same label-association gap as above, just self-inflicted this time. Fixed by passing explicit `id`s throughout `EntityFormPage`, `EntityMetadataFields`, and `WorldListPage`.

## [0.3.0] - 2026-07-10

### Added

- **Milestone 2 — Campaign Tenancy and Membership.** Campaign creation/list/switcher, campaign settings, archive/restore, invitations, role changes, member removal — both API and web UI.
- New schema: `campaigns`, `campaign_members` (unique per `campaign_id`+`user_id`, soft-removed via `status` rather than deleted so history survives), `campaign_invitations`, plus `campaign_status`/`campaign_role`/`campaign_member_status` enums.
- `membership` module: `CampaignPolicyService` encoding the §5.6 permission matrix (including its "Limited"/"Configurable" cells — a GM can't act on the owner or another GM, an Editor's GM-content visibility follows their per-member `editor_secret_access` flag), `CampaignMembershipGuard` + `CampaignRolesGuard` + `@CurrentMembership`/`@RequireCampaignRole` for campaign-scoped route authorization, invitation issue/accept/revoke with the same opaque-token pattern as email verification. See [ADR-0008](docs/decisions/0008-campaign-scoped-authorization.md) for why non-members get a 404 rather than a 403, and why this is a guard-plus-policy-service split rather than a single roles guard.
- `campaigns` module: CRUD, owner-only rename and delete, owner+GM settings/archive/restore, transactional campaign creation (creator becomes `owner` in the same transaction as the campaign row).
- New campaign invitation email (`MailService.sendCampaignInviteEmail`), reusing the verification/reset-email pattern.
- Frontend: `/app/campaigns` list+create, `/app/campaign/:id` (overview/settings/members) behind a new `RequireCampaignMembership` guard, a header campaign switcher, an invite-and-manage-members UI on the Members page, and `/accept-invitation/:token`. New `packages/ui` `Select` primitive for role pickers.
- 10 new unit tests (`CampaignPolicyService` truth table) and 18 new integration tests covering cross-campaign isolation, invitation accept/expire/revoke/email-mismatch, GM's restricted role-management scope, member removal revoking access immediately, and owner-only actions.
- `@playwright/test`, pulled forward from Milestone 13/20 (roadmap §6.2, §20.4) to verify this milestone through a real browser rather than only via `curl`. `apps/web/e2e/campaign-membership.spec.ts` drives roadmap §20.4's suite items 1–4 (register/verify, create campaign, invite, accept) plus this milestone's switcher/nav-gating/role-change/removal/archive/tenant-isolation flows end to end. Local-only for now (`pnpm --filter @worldbinder/web test:e2e`, requires `pnpm infra:up` + `pnpm dev` already running) — not yet wired into `turbo.json`'s `test` pipeline or CI, since that needs the whole stack live, which CI doesn't orchestrate yet.

### Fixed

- **`VerifyEmailPage` never resolved when driven through an actual browser** (only ever exercised via `curl`/integration tests before, which don't run React at all). It called `verifyEmail.mutate()` from inside a `useEffect`; React 19's `<StrictMode>` double-invokes effects in dev, which discards the first (real, in-flight) `useMutation` observer and mounts a fresh one that never gets `.mutate()` called on it (a `submitted` ref correctly prevented a second _network_ call, but the second _observer_ still needed one to ever leave the pending state) — so the page was stuck on "Verifying…" forever despite the API call succeeding. This was a pre-existing bug since Milestone 1, invisible until this milestone's Playwright work actually loaded the page in a browser. Fixed by switching to `useQuery` keyed on the token — the idiomatic "run once on mount" primitive, whose cache-based dedup means both the discarded and the current observer share the same result. Removed the now-unused `useVerifyEmail` mutation hook.
- **`@worldbinder/contracts`/`@worldbinder/validation` failed to load in the browser** ("does not provide an export named ...") the first time any page importing them was actually visited, for the same reason as above: nothing had ever driven the frontend through a real browser before. Vite doesn't pre-bundle pnpm workspace-linked packages by default, so it served their CommonJS `dist/index.js` raw via `/@fs/`, and native ESM import of raw CJS can't reliably detect named exports. Fixed via `optimizeDeps.include` in `apps/web/vite.config.ts`, forcing them through esbuild's CJS→ESM interop. (Left as CJS rather than switching those packages to `"type": "module"`, since the CJS NestJS API/worker also depend on them and can't `require()` an ESM module.)
- `test/jest-e2e.json` now pins `maxWorkers: 1`. With three e2e spec files sharing one real Redis instance, Jest's default parallel workers raced on the IP-scoped login rate limiter — each suite clears `ratelimit:*` at its own `beforeAll`, but concurrent suites interleaved their login volume against the same shared key, occasionally exceeding the limit mid-suite. Serial execution was the only option that didn't mean loosening a real rate limit just to make tests convenient.
- Extracted `createVerifiedUser`/`uniqueEmail`/`findEmailToken` out of `auth.e2e-spec.ts` into `test/helpers/test-users.ts` now that three suites need them.

## [0.2.0] - 2026-07-11

### Added

- **Milestone 1 — Authentication and Account Security.** Registration with email verification, login/logout, forgot/reset password, authenticated change-password, and session list/revocation — both API and web UI.
- Access/refresh token design: short-lived JWT access tokens (in-memory on the frontend, never `localStorage`) plus opaque, rotating, `HttpOnly`-cookie refresh tokens backed by `user_sessions`, with reuse detection that revokes the whole session family on replay. See [ADR-0007](docs/decisions/0007-access-token-plus-rotating-refresh-sessions.md).
- Argon2id password hashing (`PasswordService`) with opportunistic rehash-on-login when parameters change.
- Redis-backed rate limiting on register/login/forgot-password/resend-verification.
- Security event audit log (`security_events` table) covering registration, login success/failure, password changes, session revocation, and refresh-token reuse.
- Transactional email via nodemailer → Mailpit locally (verification and password-reset links).
- New schema: `user_credentials`, `user_sessions`, `email_verification_tokens`, `password_reset_tokens`, `security_events`, plus a `status` column on `users`.
- Frontend: TanStack Query + Zustand wired up for the first time (auth state, session bootstrap on app load), React Hook Form + Zod-resolver forms, first `packages/ui` primitives (`Button`, `TextField`, `FormMessage`), account pages (`/account/profile`, `/account/security`, `/account/sessions`).
- 12 new unit tests (password/token services) and 19 new integration tests covering the full auth lifecycle, invalid credentials, unverified-account rejection, expired tokens, refresh reuse, session revocation, password reset, and rate limiting.
- CI: `integration-tests` job now also runs a Mailpit service container and the required `JWT_ACCESS_SECRET`.
- Product decisions recorded ahead of Milestone 2: Timeline and the relationship graph live under **World** as views, not top-level nav destinations; players get read-only **Threads** access with field-level filtering and a projected player-facing status vocabulary distinct from the internal GM status. Reflected in `WORLDBINDER_V1_ROADMAP.md` (§3.5, §5.4, §5.6, §9.8, §10.1) and `docs/planning/ui-ux.md`.
- `CLAUDE.md` — commands, module conventions, the auth token model, and known environment footguns, so a fresh session doesn't have to re-derive them.
- `update-index` Claude Code skill (local-only) that keeps `CLAUDE.md` accurate when a documented path/command turns out stale or a real architectural addition goes undocumented.

### Fixed

- `z.coerce.boolean()` treating the literal string `"false"` as `true` (JS `Boolean("false")` semantics) — `SMTP_SECURE=false` was silently being read as `true`. Replaced with a schema that parses the literal strings.
- Nodemailer connecting to the wrong process entirely: `localhost:1025` resolved to `::1` first on this machine, where an unrelated `wslrelay.exe` was squatting on that port instead of Docker's Mailpit. Pinned `SMTP_HOST` to `127.0.0.1`.
- `cookie-parser` middleware was only registered in `main.ts`'s `bootstrap()`, which Nest's testing module (`Test.createTestingModule` + `createNestApplication()`) never calls — so refresh-cookie-dependent requests silently failed under Jest even though they worked when run manually. Moved to `AppModule.configure()` so it applies regardless of how the app is bootstrapped.
- `register()` silently no-op'd on retry for a user who existed but had never verified their email (e.g. after a prior request failed before the email sent) — now resends verification instead of the request going nowhere.

## [0.1.0] - 2026-07-10

### Added

- **Milestone 0 — Foundation.** Monorepo scaffold: pnpm workspaces + Turborepo, `apps/web` (React 19 + Vite), `apps/api` (NestJS 11), `apps/worker`, shared `packages/{contracts,validation,ui,config,eslint-config,tsconfig}`.
- Docker Compose local infrastructure: PostgreSQL, Redis, MinIO, Mailpit, all with healthchecks.
- Drizzle ORM wired up end-to-end: schema (`users` table), first generated migration, seed script.
- `GET /health` on the API reporting live PostgreSQL and Redis connectivity via `@nestjs/terminus`, with structured Pino logging across API and worker.
- Web app shell with a live API-connectivity status widget (real cross-origin fetch to `/health`, CORS configured).
- GitHub Actions CI: lint, typecheck, unit tests, build, a separate integration-test job against real Postgres/Redis service containers, a migration-drift check, and a secret scan.
- Root README with local bootstrap instructions; ADR template plus six initial architecture decision records (React/Vite, NestJS, modular monolith, PostgreSQL over MongoDB, Drizzle over Prisma, REST over GraphQL).
- Claude Code project tooling: hooks (auto-format on write, block hand-edits to generated paths, auto `pnpm install` on `package.json` changes, changelog-on-push check) and skills (`db-migration`, `new-nest-module`, `milestone-status`), all local-only (gitignored). This changelog.

### Fixed

- TypeScript/esbuild version mismatch that broke `drizzle-kit generate` (`ES2023` target unsupported by drizzle-kit's pinned esbuild).
- ESM/CommonJS interop failures between the shared `packages/*` and Jest, and between `apps/worker` and `ioredis`'s CJS types.
- Stale `vite`/`@vitejs/plugin-react` version pairing that broke the web dev server.
